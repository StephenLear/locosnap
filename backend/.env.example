# ============================================================
# LocoSnap Backend — Environment Variables
# Copy this file to .env and fill in your API keys
# ============================================================

# You need AT LEAST ONE of these two keys.
# The backend auto-detects which you have and uses the right provider.
# Having both is fine — it will prefer Anthropic for vision, specs, facts, and rarity.

# Anthropic Claude API (for Claude Vision train ID + specs + facts + rarity)
# Get your key at: https://console.anthropic.com
ANTHROPIC_API_KEY=

# OpenAI API (for GPT-4o Vision train ID + specs + facts + rarity + DALL-E 3 blueprints)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Replicate API (optional — alternative for blueprint generation)
# Get your key at: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=

# Server config
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS — update when deploying)
FRONTEND_URL=http://localhost:8081
