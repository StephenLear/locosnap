# v1.0.38 — Build Requirements

> Compiled 2026-06-04. Scope confirmed with user. Current version 1.0.37 (iOS build 59 / Android vc 29, both live).
> Two tracks: the **app build** (EAS, ships to stores) and **backend** (Render push, independent of the build).

## App build scope (EAS v1.0.38)

| # | Item | Status | Notes |
|---|------|--------|-------|
| A | Pro-upsell placement (Profile CTA elevated + Leaderboard nudge) | **DONE** — committed `f239247` | Verified, on `main`. |
| B | Manual card-edit (per-spot identity override, display-only) | **DONE** — committed `5ff1cd2` | Verified, on `main`. **Requires migration 019 applied** for the cloud write. |
| C | Blueprint 404-message cleanup | **TO BUILD** (~30 min) | Poller treats HTTP 404 "not_found" as terminal: "Blueprint unavailable — tap to regenerate", not "couldn't reach servers" + 5 wasted retries. Cosmetic half of Leon's bug (backend fix already live). `services/api.ts` `pollBlueprintStatus` + i18n. |
| D | Scanning / onboarding guide | **TO BUILD** (~2-3h) | Oula's request; validated 2026-06-04 by the "how to scan loco" TikTok search. Dismissible first-scan tips card mirroring `ProRescuePrompt` (AsyncStorage flag, analytics). New `components/ScanningGuideCard.tsx` mounted in `app/(tabs)/index.tsx` + `scan.scanningGuide.*` i18n. No backend. See scoping doc 2026-06-04-tester-feature-requests-scoping.md §1. |
| G | Rate-limit message softening (paying users) | **TO BUILD** (small) | Softer copy when a paying user hits the 60/hr backend cap. Copy/i18n only. |

**Excluded (deferred):** E — Monthly→annual upsell (proration risk on active payers; deserves its own soak → revisit for v1.0.39).

## Backend track (Render push — NOT in the app build)

| Item | Status | Notes |
|------|--------|-------|
| Vision Class 74 → Stadler FLIRT | **LIVE** (`3f73565`) | Deployed 2026-06-04. |
| Blueprint write-through task store | **LIVE** (`4967e4a`) | Deployed 2026-06-04. Leon's bug; he's been messaged to retry. |
| F — Sentry de-noise | **TO DO** | Skip `captureException` for expected 429 limit messages. Reduces noise from the 60/hr cap. |
| Loco-length BR 159 hardcode | **TO DO** | Extend `SpecsOverride` to allow `length`; add verified BR 159 figure (~confirm) + `CLASS_INVALIDATIONS` line. Data correction (not really a feature). |

## Activation checklist (in order)

1. ~~**Apply migration 019** to prod~~ — **DONE 2026-06-04** (verified `identity_override` jsonb nullable; prod at migration 019). Unblocks B's cloud write.
2. Build C, D, G; verify (`tsc` + jest); commit to `main`.
3. (Optional, anytime) ship backend track F + loco-length via Render push.
4. Bump `app.json` 1.0.37 → 1.0.38.
5. EAS production build (iOS + Android).
6. Release notes EN/DE/PL (bug-fixes first, non-financial features second, no financial offers per the release-notes rule; EULA link present for iOS).
7. Submit both stores; set Android rollout + notes; submit iOS for review.
8. Update CHANGELOG + ARCHITECTURE (build versions, migration 019 live, distribution status).

## Notes
- No scan-gate change in this build (backend gate stays at 6, frontend enforces 3 — staged; 6→3 backend flip is ~late June, separate).
- Card-edit is display-only (no leaderboard/rarity impact) by decision.
