# Handover — 2026-05-28

## Status
Complete. BR 218 + BR 246 two-loco ad shipped in DE/EN/PL with the €1 intro-offer endcard now the standing default across all three languages. EN endcard changed from £1 to €1. All assets saved (Desktop + backup drive + canonical endcard folder). No code changes — ad-asset session.

## What Was Done
- Built a clean two-loco ad (BR 218 432-3 from @trainspotting101 / Samuel + BR 246 049-2 from @gothatrainspotter / Gotha) in DE, EN, PL — 100% permitted footage, both evangelists credited.
- Footage source: `/Users/StephenLear/Desktop/samuel + gotha footage/` (Samuel "218er schlängelt..." + Gotha "Danke fürs anhalten...@Domilyx").
- Promoted the €1 intro-offer endcards from `/tmp/locosnap_de_1eur_build/` into the canonical folder and set them as DEFAULT for all future ads in all 3 languages.
- Changed the EN €1 endcard from `First month £1` to `First month €1` (patched top line only; icon/taglines untouched). Rebuilt the EN ad with it. Reason: the English ad's audience skews continental/Polish, not UK, so € is the accurate intro price.
- Drafted the two post-publish DMs (DE for Gotha, EN-context but German-language for Samuel — both are German creators), each with English translation.
- Provided DE/EN/PL captions with renewal disclosure and English translations.
- Backed up all 3 ads + source footage + build scripts to the Steve backup drive.

## What Worked
- In-place top-line patch of the endcard via `drawbox` (black box over old line) + `drawtext` re-draw, re-encoded with matching params — avoided a full rebuild and kept the icon/taglines identical. Frame-checked clean.
- Arial Black renders `€`, `£`, and Polish diacritics (ż, ą) correctly via inline UTF-8 drawtext.
- Cover-crop scaler `scale=720:1280:force_original_aspect_ratio=increase,crop=720:1280,setsar=1` eliminates black bars (except where baked into source pixels — see below).

## What Didn't Work (and Fixes)
- Beat 4 showed thin black letterbox bars on the Gotha clip at t≈12 → the bars were BAKED INTO the source pixels there, not a scaler issue. Fixed by re-windowing the Gotha beats: beat2 = ss=4 (distant approach), beat4 = ss=9 (close, bar-free). Bonus: created a better distant→close progression.

## Key Decisions
- **€1 endcards are the default going forward** (user direction 2026-05-28): use `endcard_<locale>_1eur.mp4` on ALL ads in all 3 languages. Plain `endcard_<locale>.mp4` (no price) is the fallback only when the offer must NOT appear.
- **EN endcard says €1, not £1** (user direction 2026-05-28): English-ad viewers are mostly Polish/EU, not UK. A £1 variant can be regenerated the same way if a UK-targeted ad is ever needed.
- Hook is a curiosity/comment-bait quiz ("Welche Lok ist das?" → "Und diese?"), NOT a price hook — drives distribution via identify-and-correct comments.

## Lessons Learned & Gotchas
- Letterbox bars can be baked into a creator's source clip at specific timestamps — frame-check each beat window, don't assume the scaler is at fault.
- Before posting any €1-endcard ad: confirm the Apple/Play €1 intro offer is still live (was globally active from 2026-05-26).
- EN €1 endcard now carries €, not £ — keep EN captions consistent ("first month €1, then €2.99/mo").
- The two Samuels: @trainspotting101 = German Samuel (blanket permission); @📷Samuel🇸🇰 = Slovak co-spotter (NOT covered by the blanket permission).

## Next Steps (Prioritized)
1. Post the 3 ads (DE/EN/PL) to TikTok + Instagram once the €1 intro offer is confirmed live.
2. Send the two post-publish DMs with the live link filled in (credit + link both creators).
3. Watch DE full-watch retention vs the 2026-05-26 €1 ad ceiling (20.4%) to see if the two-loco quiz format lifts it.

## Important Files
| File | Role |
|------|------|
| `~/Desktop/locosnap_br218_br246_v1_de.mp4` | Final DE ad (€1 endcard) |
| `~/Desktop/locosnap_br218_br246_v1_en.mp4` | Final EN ad (€1 endcard, rebuilt with €) |
| `~/Desktop/locosnap_br218_br246_v1_pl.mp4` | Final PL ad (€1 endcard) |
| `ad_assets/endcards/endcard_de_1eur.mp4` | Canonical DE €1 endcard (default) |
| `ad_assets/endcards/endcard_en_1eur.mp4` | Canonical EN €1 endcard — now says €1 (changed 2026-05-28) |
| `ad_assets/endcards/endcard_pl_1eur.mp4` | Canonical PL €1 endcard (default) |
| `ad_assets/endcards/endcard_{de,en,pl}.mp4` | Plain no-price endcards — fallback only |
| `/tmp/locosnap_ad_build/build_{de,en,pl}.sh` | Build scripts (also backed up to Steve backup drive) |
| `/Volumes/Steve backup/Locosnap/ad shots/BR218 BR246/` | Backup: 3 ads + source_footage/ + build_scripts/ |

## Context for Next Session
No code changes this session, so CHANGELOG.md / ARCHITECTURE.md need no edits (no frontend/backend, scan-limit, build, or distribution changes). The €1-endcard default and the EN £→€ change are recorded in the `ad_assets_endcards.md` memory file. The 3 new €1 endcard MP4s are untracked in git (`ad_assets/endcards/`) — commit them with this handover so they persist in the repo.
