# Handover — 2026-06-15

> Filename note: captures the **2026-06-15 (Monday) work day**. **No frontend/backend code changed** (git clean of code; only `docs/ARCHITECTURE.md` + a new `.claude/skills/` file). No `/changelog` entry required (changelog is for frontend/backend code only). Day was: scan-day ad posted + comments answered, a need-vs-want clapback ad built, a promotion-channel review, and a full **ASO pass** culminating in a staged iOS **v1.0.40 metadata draft**.

## Status
**v1.0.39 live both stores; prod Supabase schema 020. No new build.** The headline today is the **ASO pass**: pulled the full live listing (iOS + Play, DE/EN/PL) via Chrome, Stephen did Tier 1 (iOS promo text) + Tier 2 (Play EN/PL titles), and Claude staged **Tier 3** (iOS Name/Subtitle/Keywords + re-filled promo) into a new **iOS v1.0.40 draft — "Prepare for Submission", NOT submitted, no build attached.** Also: scan-day ad posted this morning, three dismissive comments answered, a need-vs-want clapback ad built for Tue 06-16, and a new `locosnap-ad-formats` skill created.

## What Was Done
- **Session-start audit:** read HANDOVER-2026-06-14, verified live (git clean, 0 unpushed). No stale items.
- **DE scan-day ad POSTED** (`locosnap_scanday_de_v1.mp4`) this morning — the Monday slot. No read yet; first read ~6h, real read ~24h (Tue 06-16). Logged in `tiktok_stats`.
- **Three dismissive comments answered** (mechanism/identity-first, no apology, ≤150 chars): 101_043-8 "Keiner braucht die App" (railfan handle → hobby flip); Trainspotter Nah-SH "macht die free / nicht gut" then doubled down "Brauch die trotzdem nd" → both answered with the need-vs-want flip.
- **Need-vs-want clapback ad BUILT → `~/Desktop/locosnap_clapback_de_v1.mp4`** (10.0s, 720×1280). **POSTED Tue 06-16 to TikTok + Instagram, €1 endcard kept (intro confirmed live).** Single-hero BR 232 "Ludmilla", loco-first, built from existing TrainVibez `scan.mp4`. Beats: viewfinder „Sowas braucht keiner." → RARE card „Stimmt." → card „Trotzdem stehst du am Gleis." → €1 endcard. Full pipeline + frame-check passed. Caption in `tiktok_stats`.
- **Created skill `.claude/skills/locosnap-ad-formats/SKILL.md`** — the 100 viral-format list curated through our loco-first gate (Bucket A proven/ours, B promising-if-loco-first, C traps). Invokable as `/locosnap-ad-formats` after reload.
- **Reviewed a solo-dev promotion-channel post** → lessons banked: ASO is our biggest under-exploited channel; stay organic-first (his paid took 8 months to profit; our €1 intro makes paid CAC brutal); niche micro-creator collabs fit us; our AI-assisted video pipeline is the moat against the manual-grind burnout he hit.
- **ASO pass (the big one):** pulled full live listing both stores, 3 locales, via Chrome. Findings: iOS PL **name** was English fallback; iOS DE name used English "Identifier" (no DE search volume); Play EN/PL titles were bare "LocoSnap"; promo empty everywhere; keyword fields had repeated words + space-wasting phrases. **Tier 1 + 2 done by Stephen. Tier 3 staged by Claude** into iOS v1.0.40 draft (name/subtitle/keywords reworked + promo re-filled, all 3 locales; every umlaut/diacritic verified). Full baseline + new values + the build caveat in memory `project_aso_state`.

## What Worked
- **Look-before-assert on ASC editability** — verified the live-version fields were locked rather than assuming, which correctly surfaced that Tier 3 needs a new version + build.
- **Real-keyboard typing for React fields** — synthetic JS value-setting and `form_input` both failed ASC's controlled-input validation; click → cmd+a → `computer type` worked every time. Verified each field + each Save.
- **Defensive promo re-fill** — caught that creating v1.0.40 left promo empty, which would have wiped Stephen's Tier-1 work on release.

## What Didn't Work (and Fixes)
- **`form_input` / JS value-set don't satisfy ASC React validation** → use real keyboard input (click ref → cmd+a → type). Banked for future ASC/Play console automation.
- **Play Console SPA wouldn't surface app links** (heavy Angular, account-selection redirect) → pivoted to the **public** `play.google.com/store/apps/details?id=…&hl=…` pages, which expose title/short/full per locale (Play has no hidden keyword field).

## Next Steps (Prioritized)
1. **iOS v1.0.40 will NOT ship until a build versioned 1.0.40 is attached + submitted.** `app.json` is still 1.0.39 — the next EAS iOS build MUST bump to 1.0.40 or the draft is orphaned. Also fill **"What's New in This Version"** (currently empty, required for an update) and confirm promo text persists at submit. No build triggered (awaiting Stephen's call).
2. ~~Post the clapback ad Tue 06-16~~ **DONE — posted 06-16 to TikTok + Instagram, €1 endcard kept (intro confirmed live).** 24h read due ~Wed 06-17; watch for "zu teuer" pile-on.
3. ~~Scan-day ad 24h read~~ **DONE 06-16 — FLOP.** 916 views / 3.54s avg / 8.4% full-watch / 1 follower; drop at 0:01; For You 98.6% (distributed fine = creative failure). Sits in the flop band, ties the land-grab flop. **Key lesson:** the ad WAS loco-first (BR 232 reticle frame 1) but a giant „EINEN TAG ZÜGE JAGEN" headline dominated frame 1 → read as a text-card open. Refinement banked in `feedback_loco_first_cold_open`: loco pixels aren't enough, the loco must VISUALLY DOMINATE frame 1 (shrink/drop opening headlines). Geo on-target (DE 65.6 / PL 12.7).
4. **Measure ASO lift ~2-4 weeks** after 1.0.40 ships + Play titles propagate (see `project_aso_state`).
5. **Still-open infra (non-urgent):** `storage.spot-photos` public-bucket listing fix; zero-engagement rescue push; Social Phase 1 QA.

## Important Files
| File | Role |
|------|------|
| `~/Desktop/locosnap_clapback_de_v1.mp4` | Need-vs-want clapback ad — POSTED 06-16 (TikTok + IG) |
| `~/Desktop/clapback_work/` | Build assets (build.sh + textfiles) — reusable |
| `.claude/skills/locosnap-ad-formats/SKILL.md` | NEW — 100 ad formats curated through the loco-first gate |
| memory `project_aso_state.md` | NEW — ASO baseline + change log + v1.0.40 staged values + build caveat |
| memory `tiktok_stats.md` | 06-15 entries (scan-day POSTED, comments, clapback BUILT 06-15 / POSTED 06-16) |

## Context for Next Session
- **Git: only docs uncommitted at handover write-time (this file + ARCHITECTURE.md + the new skill); no code changed.** Backend unchanged.
- **The live ASO state:** Tier 1 + 2 are LIVE-pending (promo no-review; Play titles in content review). **Tier 3 is a DRAFT in iOS v1.0.40, not submitted** — it needs a 1.0.40 build.
- **Tue 06-16:** clapback ad POSTED (TikTok + IG). Open reads: scan-day 24h read (due 06-16) + clapback 24h read (due ~Wed 06-17).
- **Do NOT trigger any build without explicit confirmation** (`feedback_build_approval`).
