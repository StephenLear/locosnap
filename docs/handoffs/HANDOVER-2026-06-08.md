# Handover — 2026-06-08

## Status
Content + ops + one backend fix. **Built the DE BR 140 "aspiration" paper-diorama ad (post-ready, NOT posted), shipped + pushed a BR 140 facts-layer fix to Render, captured the BR 218 24h read (format validated), and handled the 611 ad comment threads (Dieselpower Dosto→648 misID + a 4-volley Lukas competitive thread).** `CHANGELOG.md` + `ARCHITECTURE.md` updated and pushed. One backend commit (`85cdff7`) is LIVE on Render. No app build this session.

## What Was Done
- **DE BR 140 aspiration ad BUILT** → `~/Desktop/locosnap_140_paper_de_v1.mp4` (10.0s, 720×1280, **NOT POSTED**). Decision flow: DE+PL pair + collection/rarity-aspiration angle (user-chosen). New visual register — the WW1-game-style **museum-diorama-on-a-plinth** newspaper diorama (cross-section ballast base, cool/moody industrial, green loco as colour accent), generated via the `newspaper-diorama` skill. Source still `~/Desktop/BR140/BR140 diaroma.png` (masthead "DIE ABENDPOST" / headline "STAHL UND STROM" — both rendered clean). Beats: (1) 0–3.2s diorama + hook "ERWISCHST DU EINE LEGENDE?" (Gemini sparkle `delogo`'d); (2) 3.2–6.0s loco punch-in + "879 GEBAUT / AB 1957"; (3) 6.0–8.0s **real in-app LEGENDARY reveal** from today's live recording (BR 140 · PRESS · 110 km/h · 3,700 kW); (4) 8.0–10.0s €1 first-month endcard. All beats frame-checked. Caption drafted (DE, 5 hashtags, no emoji, no hard fleet-count).
- **BR 140 facts-layer fix SHIPPED + PUSHED** (`85cdff7`, LIVE on Render). `trainFacts.ts` had **no BR 140 bullet**, so the reveal's facts prose hallucinated the standard-gauge mainline 140 onto PRESS's 750 mm narrow-gauge **Pressnitztalbahn** heritage/museum line (contradicting its own correct spec card). Added a BR 140 / E 40 trainFacts bullet (West German Bundesbahn Bo'Bo' four-axle, 1957–73, KM/Krupp/Henschel/SSW, 879, 110 km/h, 3,700 kW, standard gauge; forbids narrow-gauge/museum/six-axle/Siemens-EuroSprinter/three-phase-AC/1990s-reunification framing) + 8 `CLASS_INVALIDATIONS` keys (`2026-06-08T23:59:00Z`). tsc clean, 235/235 tests. **Structured specs were already correct** (fixed by someone since the stale 2026-04-19 recordings, which had six-axle / Siemens-EuroSprinter / 6,360 kW / 120 km/h / 1990s hallucinations).
- **BR 218 paper-diorama 24h read captured** (screenshots `~/Desktop/218 24hr/`): **614 views, 18.5% full-watch, 80.9% DE lock, 5.85s avg, For You 96.4%, 2 follows.** Upper DE band (ceiling 19–21%, flop tier 10–12%); DE lock the best of any recent DE ad. **Paper-diorama format VALIDATED for DE.** Logged to `tiktok_stats`.
- **611 ad comments handled** (all replies posted, logged to `tiktok_stats` 06-08):
  - **Dieselpower.De_GermanRails®** — ribbed the "Jeden Zug erkennen" slogan with a real misID (a 4th-gen Doppelstockwagen returned **BR 648**). Conceded with humour, committed to a fix ("Bau ich nach"). Logged to `backend_backlog_corrections` as **PENDING REPRO** — Stephen to scan a real 648 (control) + a Dosto/Steuerwagen (the actual repro) before any rule ships.
  - **Lukas** (likely the same Lukas from the 2026-04-28 Google-KI-Modus validation) — 4-volley competitive thread ("alles KI / Wikipedia better" → "Und wie Wikipedia das tut" → "your replies are AI" → "any AI can scan a train"). Closed with two NEW canonical DE rebuttals (persistence: "ChatGPT vergisst dich nach der Antwort"; no-training flip: "viele KIs raten bei Baureihen, weil sie nie darauf trainiert wurden") + the non-native-speaker AI concession. Thread CLOSED (stop-rule). Banked in `project_competitive_positioning`.

## What Worked
- **"Scan it live first" before driving ad traffic** — the pipeline rule paid off hard. The 7-week-old BR 140 reveal recordings would have shipped an ad showing "Siemens EuroSprinter / six-axle / 1990s reunification." Today's live recap showed specs are now fixed AND surfaced a *residual* facts bug. Never trust stale recordings or memory for app state.
- **`newspaper-diorama` skill + WW1-aesthetic transfer** — the plinth/cross-section register produced a strong, distinctive DE artifact first try; headline rendered clean (short, no-umlaut words).
- **Verify-first on Gotha/Samuel 140 footage** — memory already said neither had 140; confirmed Path A (Gemini, no footage) sidesteps the whole permission/takedown risk that killed BR 140 v5.
- **Concede-don't-deny on the "your replies are AI" jab** — the non-native-speaker translation truth disarmed Lukas's gotcha.

## What Didn't Work (and Fixes)
- **Stale BR 140 reveal recordings (2026-04-19)** showed gross hallucinations → checked the LIVE app instead; specs were fixed, but found the residual narrow-gauge facts bug → fixed + pushed.
- **Web search / WebFetch for TikTok channel footage** — dead ends (TikTok not indexable/scrapable). The reliable path is the connected Chrome extension (not paired this session) or memory inventories.

## Key Decisions
- **DE BR 140 ad = aspiration/rarity angle, single-hero, Gemini Path A (green diorama), €1 endcard.** Left the loco GREEN (not regenerated in PRESS blue) — the green heritage diorama is the stronger artifact; the green-illustration → blue-real-scan shift reads fine.
- **Pushed the BR 140 facts fix to Render** (user call) — so the live app is clean before the ad drives 140 scans; the ad itself didn't depend on it (reveal beat shows only the correct front card).
- **DE + PL pair for tomorrow** — DE built; **PL (Newag Dragon, RARE, annual endcard) still to build** and needs a fresh LIVE Dragon scan recording first.

## Lessons Learned & Gotchas
- **A class can have correct KNOWN_SPECS but still hallucinate in the free-form facts prose** if it has no `trainFacts.ts` bullet (BR 140 = the latest case; family: BR 114/628/ÖBB 4020/Sr1). When auditing a class for an ad, check BOTH layers.
- **PRESS is two operations:** the 750 mm narrow-gauge Pressnitztalbahn museum line AND a standard-gauge mainline freight/loco-hire arm (runs ex-DB 140s). The AI conflated them — do not.
- **"Jeden Zug erkennen" is an over-absolute slogan** (same trap family as 218 "verschwindet vom Netz") — soften on the NEXT endcard render; left the posted 611 ad as-is.
- **BR 140 ad history:** ran 19 Apr, 8 May, 9 May (v5 "Wirtschaftswunder", TAKEN DOWN 10 May for using Bahnen-in-Ostbayern footage), and in the 3 Jun haul. Backup footage clips have creator names baked in — never reuse; Gemini Path A only.

## Next Steps (Prioritized)
1. **Post the DE BR 140 ad** (`~/Desktop/locosnap_140_paper_de_v1.mp4`) + caption; capture its 24h read.
2. **Build the PL aspiration ad** (Newag Dragon, RARE, annual endcard) — get a fresh LIVE Dragon scan recording from Stephen first (scan-it-live rule).
3. **Capture the 611 retrospective ad 24h read** (~06-09) — same paper-diorama format, direct retest of the 218 result.
4. **Dosto→648 repro** — Stephen to scan; then ship the vision rule + close the public "Bau ich nach" promise.
5. **Confirm the BR 140 facts fix live** — re-scan a 140 (or re-poll `/api/health`) to verify the narrow-gauge line is gone after the Render deploy completes.

## Important Files
| File | Role |
|------|------|
| `~/Desktop/locosnap_140_paper_de_v1.mp4` | DE BR 140 aspiration ad — POST-READY, not posted. |
| `~/Desktop/BR140/BR140 diaroma.png` | Gemini source diorama still (green BR 140, "STAHL UND STROM"). |
| `~/Desktop/BR140/ScreenRecording_06-08-2026 21-49-11_1.MP4` | LIVE BR 140 scan-reveal used for beat 3 (clean front card). |
| `~/Desktop/218 24hr/` | BR 218 24h-read screenshots (18.5% / 80.9% DE lock). |
| `backend/src/services/trainFacts.ts` | BR 140 facts bullet added (`85cdff7`, LIVE). |
| `backend/src/services/trainCache.ts` | 8 BR 140 `CLASS_INVALIDATIONS` keys (`85cdff7`, LIVE). |
| `tiktok_stats` / `backend_backlog_corrections` / `project_competitive_positioning` (memory) | All updated this session. |

## Context for Next Session
- **Backend commit `85cdff7` is LIVE on Render** (pushed; `git log origin/main..HEAD` empty). Render auto-deploy was in progress at session close (~2–3 min build) — re-scan a 140 to confirm clean facts.
- **No app build this session.** v1.0.38 LIVE on both stores (unchanged).
- **Tomorrow's plan:** DE+PL aspiration pair. DE (BR 140) built + post-ready; PL (Dragon) still to build — needs a live Dragon scan recording.
- **Two open public commitments:** the Dosto→648 "Bau ich nach" promise (pending repro), and nothing outstanding on the Lukas thread (closed).
- **Chrome extension NOT paired** this session — if channel browsing is needed next time, ask Stephen to connect it.
