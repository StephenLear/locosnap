# Handover — 2026-06-24

## Status
**v1.0.40 "Spotting Atlas" is now LIVE on BOTH stores** (Apple approved iOS build 63 today; Android live since 06-23). Two backend Finnish-diesel corrections shipped for tester Oula (Dr19 misID + Dr16 operator/data). BR 111 DE tier-debate 24h read came in below band (hero-motion problem, logged). Atlas ads remain unposted (HOLD now lifted — Stephen's to post). **2 commits unpushed at session end** — one of them (`5ccdb16`) needs a Render push to deploy the Dr16 fix.

## What Was Done
- **Audited the 06-23 handover.** Found it already existed and was comprehensive (built end-to-end Atlas ads, stats review, vision fallback fix) — did NOT overwrite. Recorded the Atlas-ad HOLD in its Status + Next Step #1. Note: no separate 06-23 file had been *thought* missing, but three 06-23 commits had landed after it was written.
- **Reviewed 24h ad stats** (`~/Desktop/Stats 24:6/`, 14 screenshots):
  - **BR 111 DE tier-debate — TikTok 24h: 567 views / 4.19s avg / 11.6% full-watch / 0 new followers / died 0:01.** BELOW the tier-debate band floor (15.5%); weakest tier-debate read to date, below even the BR 101 gacha (14%). Search traffic 13.8% (unusually high). Geo DE 54.5 / PL 15.5.
  - **BR 111 — Instagram: 123 views / 3s avg / skip 63.5%** — also weak. Underperformed on BOTH platforms.
  - **BR 101 Köln gacha — Instagram: 1,442 views / 6s avg / skip 43.5% / 9 saves / 7 shares / 4 follows** — the strong one; matches its healthy TikTok read.
  - **Verdict: hero-motion problem, not a format failure.** Gacha BR 101 (lateral departure) beat tier-debate BR 111 (misty head-on) on both platforms. Logged to `tiktok_stats` (new 2026-06-24 entry) + added a frame-1-motion refinement to `feedback_loco_first_cold_open`.
- **Backend — VR Dr19 + Dr16 corrections (tester Oula), web-verified, shipped (`53b2db6`, PUSHED + live on Render):**
  - **Dr19 was being identified as Dv12.** Added a dedicated `vision.ts` rule (modern Stadler vs classic Dv12), `KNOWN_SPECS`, `KNOWN_RARITY` ("uncommon"), `trainFacts.ts` block, `trainCache.ts` invalidation. Specs: Stadler Rail Valencia, 2× Caterpillar C32 = **1,900 kW**, 120 km/h, 88 t, **Bo'Bo'**, 60 ordered (in service since May 2023), freight.
  - **Dr16 "Iso Vaalee" had no data** + a WRONG axle claim in the old Dv12 rule ("Co'Co' six-axle"). Corrected to **Bo'Bo' four-axle**, single off-centre cab; Valmet/Transtech 1985–1992, 23 built; rarity **"epic"**. Added specs/rarity/facts/cache.
  - Also fixed the Dv12 rule's secondary error (Dr19 described as "cabs at both ends" → single central cab).
- **Oula follow-up (same session): VR has FULLY withdrawn the Dr16; ArcticRail is now the SOLE operator (2 units).** Web-corroborated (ArcticRail Wikipedia confirms two). Shipped `5ccdb16` — updated `vision.ts` + `trainFacts.ts` + `rarity.ts` so any in-service Dr16's operator is **ArcticRail** (not VR); bumped `dr16` cache invalidation to `2026-06-24T15:00:00Z`. **This commit is NOT yet pushed → not yet on Render.**
- **Apple approved iOS v1.0.40 (build 63) today** → v1.0.40 now LIVE on both stores. Updated `docs/ARCHITECTURE.md` header (`db27b15`) + `project_status` memory. Clears the Atlas-ad HOLD and fully unblocks the v1.0.40 ASO draft.
- **Drafted a short IG-length reply for Oula** (both fixes in two sentences) — in chat, ready to send once `5ccdb16` is pushed/live.
- tsc clean + **268/268 backend tests** pass throughout both backend commits.

## What Worked
- **Web-verifying tester claims before shipping** (Wikipedia / Railway Gazette / Stadler) — caught that Dr19 is Bo'Bo' (not "cabs at both ends") and that the existing Dr16 "Co'Co'" claim was wrong. Also confirmed Oula's "two ArcticRail units" over the lagging Wikipedia "one".
- **`Explore` subagent to map all Finnish-class handling** across vision/specs/rarity/facts/cache before editing — gave an exact gap table (which class had what), so the fix was precise.
- **Per-class cache invalidation** (no global bump) per `backend_cache_invalidation_pattern` — bumping just the `dr16` keys to T15:00 to re-render anything cached after the morning push.
- **Reading IG and TikTok separately** for the same creative — but this time BOTH agreed (BR 111 weak on both), confirming a subject/hero issue rather than a platform-fit one.

## What Didn't Work (and Fixes)
- **BR 111 DE tier-debate underperformed** (11.6% full-watch, below band) → diagnosed as a hero-motion failure: a foggy head-on reads near-static and bails at 0:01 even with the on-screen debate. Fix is forward-looking: next DE tier-debate must use a laterally-moving hero (departure/pass). Logged.
- **Existing vision rule had Dr16 as "Co'Co' six-axle"** (wrong) → corrected to Bo'Bo' four-axle; this broken axle-count cue could itself have caused misIDs.
- **My initial "one ArcticRail unit" hedge** (from Wikipedia) was the lagging figure → Oula's "two" was right; firmed up after a corroborating web check. Logged in `backend_backlog_corrections` (primary-source tester beat the encyclopedia).

## Key Decisions
- **Did not overwrite the existing 06-23 handover** — it was richer than any git-only reconstruction (had the Desktop-only ad assets + comment threads). Edited in place instead.
- **Shipped both Oula fixes same-session** (the established tester-fix-loop pattern), with web verification first per `feedback_verify_premise_before_overriding_expert`.
- **Atlas ads stay Stephen's to post** — HOLD condition (iOS live) is now met, but posting is the user's action, not mine.

## Lessons Learned & Gotchas
- **Frame-1 MOTION is now the third loco-first gate** — present + dominant + visibly moving *laterally*. A head-on/near-static hero fails even if loco-first and loco-dominant. Captured in `feedback_loco_first_cold_open`.
- **The tier-debate format does NOT rescue a low-motion hero** — it wins where the hero moves (ET22 PL 15.8%, EN57 PL 20.7%), not on subject/format label alone.
- **A primary-source tester on the ground can be more current than Wikipedia** — trust the tester for live operator/fleet state, verify the figures.
- **No untracked critical files** this session — working tree clean apart from the two committed-but-unpushed commits below.

## Next Steps (Prioritized)
1. **Push `db27b15` + `5ccdb16`** — `5ccdb16` deploys the Dr16/ArcticRail correction to Render (then the Oula IG reply becomes fully true and can be sent). **Backend commit pending a Render push.**
2. **Post the Atlas ads** (HOLD lifted) — Flagships first, then Workhorses; EN/DE/PL each; **add music at posting** (cuts have no audio); captions in `tiktok_stats` 2026-06-23 entry. `~/Desktop/atlas_ads/{flagships,workhorses}/`.
3. **Apply the v1.0.40 ASO staged metadata** (`project_aso_state`) — now fully unblocked.
4. **Verify Atlas renders** in the store-downloaded build on BOTH platforms (Maps key has both SHA-1s — verification, not a fix; if blank, check key Android restrictions first).
5. **Confirm the 06-23 vision 500/503 fallback** is live on Render and correct the stale "Not yet deployed" line in the 2026-06-23 changelog block.
6. **Watch:** Dr19/Dr16 re-scan confirmations from Oula; whether the next DE tier-debate with a laterally-moving hero clears the band.

## Important Files
| File | Role |
|------|------|
| `backend/src/services/vision.ts` | Dr19 (new rule) + Dr16 (Bo'Bo' correction, ArcticRail sole operator) disambiguation |
| `backend/src/services/trainSpecs.ts` | Dr19 + Dr16 `KNOWN_SPECS` entries (web-verified) |
| `backend/src/services/rarity.ts` | Dr19 "uncommon" / Dr16 "epic" |
| `backend/src/services/trainFacts.ts` | Dr19 + Dr16 facts blocks (Dr16 = ArcticRail-only) |
| `backend/src/services/trainCache.ts` | Dr19/Dr16 `CLASS_INVALIDATIONS` (dr16 bumped to T15:00) |
| `docs/ARCHITECTURE.md` | Header now says v1.0.40 LIVE both stores + Dr19/Dr16 deploy status |
| `docs/CHANGELOG.md` | 2026-06-24 backend block (Dr19/Dr16 + same-day Dr16 follow-up) |
| `docs/handoffs/HANDOVER-2026-06-23.md` | Prior session (Atlas ads built, vision fallback); ad-HOLD + BR 111 read folded in |
| `~/Desktop/atlas_ads/{flagships,workhorses}/` | Built Atlas feature-reveal ads (Desktop, not in repo) — postable now |
| `~/Desktop/Stats 24:6/` | The 24h ad-stat screenshots reviewed this session |

## Context for Next Session
- **2 commits unpushed:** `db27b15` (docs, architecture iOS-live) + `5ccdb16` (backend Dr16 follow-up). **`5ccdb16` needs a push to go live on Render** — it deploys the "ArcticRail is sole Dr16 operator" correction. `53b2db6` (the first Dr19/Dr16 batch) is already pushed + live.
- **Oula IG reply (short version), ready to send once `5ccdb16` is live:** "Thanks for all of this — you nailed both. Fixed the Dr19 (it was reading as a Dv12) so it's now its own class, and updated the Dr16 to show ArcticRail as the only operator and rate it as a rare catch. Both live shortly. Kiitos!"
- **Memory updated this session:** `tiktok_stats` (2026-06-24 BR 111 read), `feedback_loco_first_cold_open` (frame-1-motion refinement), `backend_backlog_corrections` (Dr19/Dr16 + ArcticRail update), `project_status` (v1.0.40 live both stores), `pricing_localisation`/others untouched.
- **Verified figures:** Dr19 = Stadler Valencia, 2× Cat C32 = 1,900 kW, 120 km/h, 88 t, Bo'Bo', 60 ordered, freight, in service since May 2023. Dr16 = Bo'Bo' (NOT Co'Co'), Valmet/Transtech 1985–1992, 23 built, ~11 scrapped, VR fully withdrawn 2026, ArcticRail runs 2 restored units.
