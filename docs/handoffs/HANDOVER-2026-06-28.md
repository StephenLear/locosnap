# Handover — 2026-06-28

## Status
Productive mixed day: diagnosed the download drop (it's a GERMANY problem, not global), built + posted a DE ICE 1 rescue ad from a new permission-clean creator (thattrainbro), planned the PL ST44 "Gagarin" follow-up (footage outreach to @_mikolkaa sent over TikTok, awaiting reply), and shipped + deployed a backend ICE 1 facts/type fix. Working tree clean, nothing unpushed.

## What Was Done
- **Stats review → root-caused the download drop as a GERMANY collapse.** Google Play (28-day): device acquisitions ▼30%, first opens ▼41%, MAU ▼6%. Apple territory split was the key: **Germany ▼68% (76→24 units)** while **Poland ▲96%** and **UK ▲108%** — the −52 DE units ≈ the entire aggregate iOS drop (151→110). The "−30%" was hiding two markets surging and one collapsing.
- **Diagnosed the Atlas ads as the format drift.** Pulled live TikTok per-video reads: Atlas Workhorses DE **4.4%** full-watch / 3.04s, Flagships DE **6.9%** / 6.32s, Flagships PL **7.8%** / 3.89s (all died 0:01, distributed fine = creative failure). vs the tier-debate band (15.5–20.7%). The Atlas feature-reveal format abandoned the proven loco-first tier-debate engine. PL Workhorses (28 Jun) too early to read.
- **thattrainbro confirmed as a permission-clean DE footage source.** @thattrainbro (Prince Yona RLC), verified DE spotter + LocoSnap's first Lifetime member. DM exchange confirmed blanket permission for his OWN clips with link + credit ("Cool, wir können das machen"). Logged `footage_source_thattrainbro`.
- **Built + posted the DE ICE 1 rescue ad** (`~/Desktop/ICE1/locosnap_ice1_de.mp4`, 10.1s, 720×1280). PIVOTED from ICE 2 → ICE 1 mid-build (see below). Hero = his "Basel" ICE 1 LDV clip (curve sweep + driver wave, in-point ~8.0s so the cab is large from frame 1). Beats: hero+hook „Kennst du noch den ersten ICE?" → scan → real EPIC · BR 401 card (EMU line masked) + „Modernisiert bis 2030+ ... Verdient EPIC?" → €1 endcard. Stephen posted as-is and thanked thattrainbro (DM drafted).
- **Researched ICE storylines** (subagent): the killer hook = "the ICE 1 outlives its successor" (ICE 2 scrapped by 2027; ICE 1 LDV'd to 2030+) — verified, true.
- **Planned the PL ST44 "Gagarin" ad + sent footage outreach.** Researched ST44: NOT near-gone (still daily PL service, LHS 32 M62, withdrawal ~2029) so framing = "going, not gone"; top viral levers = cold-start clag + two-stroke roar → KEEP RAW AUDIO (exception to the silent template). No ST44 footage on the backup drive; EN57/ET22 clips there are third-party + recently-used. Found @_mikolkaa (IG+TT, Polish ST44, 2,047-like Turkol reel) — **permission DM sent over TikTok, awaiting reply.** Full plan in `footage_source_mikolkaa`.
- **Shipped backend ICE 1 facts/type fix** (commit `9ae7707`, pushed to main → Render auto-deploys; docs `fb4c034`). See below.

## What Worked
- **Reading the Apple territory split, not the aggregate.** The headline −30% looked like global decline; the per-country view showed PL/UK surging and Germany alone collapsing — completely different action (Germany rescue, don't touch PL/UK).
- **The footage-source → research → build → scan-verify pipeline** for the DE ad. Frame-sampling the downloaded clip with ffmpeg contact sheets to pick the hero in-point is the right way to verify motion (not squinting at the web player).
- **Pivoting the ad to ICE 1 when the app misID'd the ICE 2** — turned a blocker into a better, app-correct story (ICE 1 EPIC, and the "outlives its successor" hook works from the ICE 1 side).

## What Didn't Work (and Fixes)
- **ICE 2 (BR 402) scanned as ICE 1 (BR 401)/EPIC.** Intrinsic limit: ICE 1/2 share the rounded nose; the only front-on discriminator (Scharfenberg flap size / "402" number) wasn't resolvable in a moving 3/4 shot, so the model fell back to the famous + statistically-dominant ICE 1. → Pivoted the ad to genuine ICE 1 footage (which the app IDs correctly). Logged to `backend_backlog_corrections` as an intrinsic limit + the missing ICE 2 grounding.
- **ICE 1 facts/type were wrong even on the correct ID** — card showed type "EMU" and facts claiming "first purpose-built high-speed EMU", "distributed-power traction across 14 cars", and the 406.9 km/h world record. → FIXED + SHIPPED: `trainFacts.ts` new BR 401 block (power-car-hauled, not EMU; ICE-V owns the 406.9 record), `vision.ts` ICE 1/2 type pinned (never EMU), `trainCache.ts` invalidation bumped to 2026-06-28. tsc clean, 270/270 tests. For the ad we masked the "· EMU" line and kept those facts off-screen.
- **drawtext rendered tofu boxes for `\n` in a multi-line textfile.** → Fix: render each line as a separate `drawtext` filter (per-line), not one textfile with newlines.
- **lokfuhrer233 (great ST44 footage) is YouTube-only** — no TikTok/IG/email/website (checked About panel). Only outreach = a public YouTube comment. → Pivoted to finding a DM-able creator (@_mikolkaa) instead.

## Key Decisions
- **Treat the slump as a Germany rescue, not a global reset.** PL/UK are winning on tier-debate — keep that playbook; fix Germany's content.
- **Drop the Atlas feature-reveal format; return to loco-first tier-debate** with a close, laterally-moving hero. Stop posting further Atlas cuts.
- **Pivot the DE ad to ICE 1** (app-correct + better story) rather than fight the ICE 2 misID or fake a card.
- **For the ST44 PL cut, keep the raw two-stroke engine audio** — deviation from the silent template, justified because the sound IS the hook.
- **Frame ST44 as "going, not gone"** — it's NOT near-retirement (unlike the ICE 2); no false scarcity.

## Lessons Learned & Gotchas
- **Frame-1 must be CLOSE/LARGE, not a distant approach** — a distant train (even moving) causes the big 0:01 drop. Saved as the new four-part frame-1 test in `feedback_loco_first_cold_open` (present + dominates-over-text + moves-laterally + CLOSE/LARGE held >2s).
- **ICE is the hardest vision family** — most rule-patched class in `vision.ts`; ICE1↔ICE2 front-on can't be reliably separated without a readable fleet number. Don't build ICE 2 ads from ambiguous footage.
- **Verify a creator is DM-able before drafting outreach** — lokfuhrer233 wasted a step by being YouTube-only.
- **The EN57/ET22 clips on the backup drive are third-party (hashtag filenames = scraped)** — using them reintroduces the BR 140 takedown risk; prefer permission-clean sources.

## Next Steps (Prioritized)
1. **Await @_mikolkaa's TikTok reply** → on yes, vet their ST44 clips (Turkol curve one first), scan-verify the card, build the PL "Gagarin" cut (keep raw audio; plan in `footage_source_mikolkaa`). If no reply in a day or two, line up a backup PL creator from the IG #st44 grid.
2. **Verify the ICE 1 backend fix live** once Render finishes — fresh ICE 1 scan should read type "High-speed train" (not EMU) with corrected facts.
3. **Watch the DE ICE 1 ad's read** (posted 28 Jun) — does it clear the tier-debate band and arrest the German slide? This is the Germany-rescue test.
4. **Germany is the download priority** — DE ▼68% on iOS; keep feeding DE loco-first tier-debate (thattrainbro is now a clean DE source for fresh heroes).
5. **Do NOT post more Atlas cuts.** Let the PL Workhorses (28 Jun) read out; don't add to the format.
6. **Optional/when relevant:** ICE 2 (BR 402) backend grounding only if committing to ICE 2 ads; backup PL creator outreach.

## Important Files
| File | Role |
|------|------|
| `~/Desktop/ICE1/locosnap_ice1_de.mp4` | The DE ICE 1 rescue ad (posted 28 Jun) + `_caption.txt` alongside |
| `~/Desktop/ICE1/clip2_raw.mp4` | thattrainbro ICE 1 "Basel" LDV hero source (driver wave) |
| `backend/src/services/trainFacts.ts` | Added BR 401/ICE 1 facts block (the fix) |
| `backend/src/services/vision.ts` | ICE 1/2 type pinned to power-car-hauled, never EMU |
| `backend/src/services/trainCache.ts` | ICE 1 cache invalidation bumped to 2026-06-28 |
| `docs/CHANGELOG.md` | 2026-06-28 entry for the ICE 1 fix |
| `docs/ARCHITECTURE.md` | "Last updated" header now 2026-06-28 (ICE 1 fix) |
| `memory/footage_source_thattrainbro.md` | DE source — blanket permission, credit @thattrainbro |
| `memory/footage_source_mikolkaa.md` | PENDING PL source + full ST44 ad plan |
| `memory/backend_backlog_corrections.md` | ICE 1 marked SHIPPED; ICE 2 grounding/twin-ID still open |

## Context for Next Session
- **Two ads this cycle:** DE ICE 1 (built + posted today); PL ST44 Gagarin (blocked on @_mikolkaa footage permission, DM sent over TikTok).
- **Backend ICE 1 fix is pushed** (`9ae7707`) — Render auto-deploys from main; verify with a re-scan when convenient.
- **The strategic frame:** downloads are down because of Germany specifically; PL/UK are healthy. Every DE content decision should aim to recruit DE viewers with loco-first tier-debate.
- All replies/DMs this session were drafted by us and sent by Stephen. Nothing queued on our side except the @_mikolkaa reply (third party).
- Handover dated 2026-06-28 per the system clock; one file per work day (update in place, not -2).
