# Handover — 2026-04-20 (session 2, late evening)

## Status
Tester support only — one 1:1 reply sent to Steph the Spotter (Stephstottor@gmail.com) about a reinstall problem after she uninstalled the previous LocoSnap version. No code changes, no builds, no deploys. All pending work from HANDOVER-2026-04-20.md still stands.

## What Was Done
- Diagnosed Steph's reported issue: she could not download/install the v1.0.19 APK after uninstalling the prior version. She had also flagged the same on TikTok and email.
- Drafted a short English troubleshooting reply covering the three most likely sideload-reinstall failure modes: (1) re-tap the link, (2) enable "Install unknown apps" for Chrome/Gmail, (3) bypass Play Protect via "More details → Install anyway", (4) request screenshot if download itself fails.
- Sent via Resend: id `8a2c3286-7510-4b75-81e1-7470bcb61650`, CC `unsunghistories@proton.me`, from `stephen@locosnap.app`, User-Agent header set correctly.
- Logged the send in `memory/tester_contacts.md` under a new "1:1 Replies" table.

## What Worked
- Memory lookup flow: `tester_contacts.md` already had Steph's address and her Resend history; `project_status.md` confirmed she's on v1.0.19 APK from the 2026-04-19 "please update" nudge; no re-asking the user for credentials.
- Resend key read from `backend/.env` per `credentials_locations.md` — no re-prompt.
- File-based JSON payload for curl (`/tmp/steph_reply.json`) kept the multi-line HTML body clean and avoided shell-escaping issues with apostrophes in the body copy.

## What Didn't Work (and Fixes)
- Nothing broke this session.

## Key Decisions
- **Troubleshoot before resending anything.** Did not generate a new APK email or trigger any build. The 2026-04-19 mass-nudge link is still live, so the right first move is to fix her local install flow, not send a duplicate artifact.
- **No ARCHITECTURE.md edit this session.** The "last updated" stamp is correct for 2026-04-20 evening (set earlier today for the BR 232 override + AAB build). A 1:1 tester reply is not architecture-doc-worthy — tester list / Pro grant / build state did not change.
- **No CHANGELOG.md edit.** Per the rule in CLAUDE.md, the changelog tracks frontend/backend code changes only. Nothing in `frontend/` or `backend/` was touched this session.

## Lessons Learned & Gotchas
- When a tester says "can't download the new version", first check the last mass-send in `project_status.md` — it tells you which build link they're clicking and whether it's still within the EAS artifact TTL. Saves asking them to clarify.
- Reinstall-after-uninstall failures on Android sideload are almost always one of: Play Protect warning, "install unknown apps" permission lapsed for the source app (Chrome/Gmail), or a signing-key mismatch. The first two are user-side and fixable in the troubleshooting reply. A signing-key mismatch would need a fresh APK from the same EAS credential — not the case here because she got the 2026-04-19 APK from the same credential as her original install.
- Handover file already existed for today (`HANDOVER-2026-04-20.md`), so this one is `-2` per the filename rule.

## Next Steps (Prioritized)
1. **Verify the v1.0.20 AAB and submit to Play Store** — build id `386f0d23-a8a7-49a5-a387-d1c579e10233`, held from auto-submission pending morning verification on 2026-04-21 (final day of 14-day tester window). Staged rollout 20% recommended.
2. **Post BR 232 "Ludmilla" DE organic video** — `~/Desktop/locosnap_br232_de.mp4` is ready for the 2026-04-21 AM slot on TikTok first, Instagram Reel ~1h later.
3. **Watch for Steph's reply.** If she confirms the Play Protect / unknown-sources steps worked, close the thread. If the download itself fails, get a screenshot and decide whether to resend the APK link or investigate an EAS artifact TTL expiry.
4. **Sm2 / Sm4 / Sm5 Finnish commuter-EMU deeper fix** — queued for next backend session (not launch week). See HANDOVER-2026-04-20.md for the vision.ts / trainSpecs.ts plan.
5. **9 outstanding Pro grants** — dieterbrandes6, jlison1154, krawiec.jr69, m.j.griffiths.ucl, mike.j.harvey, muz.campanet, qwertylikestrains, scrtrainmadother, trithioacetone. Automated monitor runs every 4 hours.

## Important Files
| File | Role |
|------|------|
| `memory/tester_contacts.md` | Updated this session — new "1:1 Replies" table with Steph's troubleshooting send (Resend id `8a2c3286-7510-4b75-81e1-7470bcb61650`) |
| `backend/.env` | Contains `RESEND_API_KEY` — read on every tester send, never re-ask |
| `docs/handoffs/HANDOVER-2026-04-20.md` | Prior handover covering the main work of the day (BR 232 / Ludmilla ad, AAB build, Resend key persistence) |
| `docs/ARCHITECTURE.md` | Unchanged this session; last updated stamp 2026-04-20 evening still accurate |
| `docs/CHANGELOG.md` | Unchanged this session; no frontend/backend code edits |

## Context for Next Session
- **Android v1.0.20 AAB sits unsubmitted.** Do not auto-submit — wait for explicit user verification on the morning of 2026-04-21.
- **Steph thread is open.** If she replies that the troubleshooting worked, close. If she comes back with a specific error, the next likely action is either (a) walk her through exact Android settings with a screenshot, or (b) resend the v1.0.19 APK link in case her download cache is corrupted. Do NOT build a new APK for her alone — the next Android artifact is the v1.0.20 AAB going to Play Store.
- **EAS credits on pay-as-you-go.** Never trigger a build without explicit in-session approval.
- **Untracked files noted by `git status --short`** are all pre-existing (`.claude/`, prior handoffs `HANDOVER-2026-04-14.md` through today). None are build-critical (no `patches/`, no `eas.json`, no config files referenced by `package.json`). Not a blocking issue for any pending deploy.
- **No German / Polish / Finnish copy produced this session** — Steph is a UK tester, reply was English only.
