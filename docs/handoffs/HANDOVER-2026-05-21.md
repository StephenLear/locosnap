# Handover — 2026-05-21

## Status

Substantive content + maintenance session. Two backend commits pushed to main (`2a5db4e` SU46 builder fix, `ffcec5c` architecture doc sync) — Render auto-deploy in flight at session close. Polish endcard restored at canonical path and saved to memory with reproducible script. SU46 PL ad built end-to-end (file ready at `~/Desktop/locosnap_su46_pl.mp4`), caption finalised to TikTok's new 5-hashtag limit, awaiting user post. V 200 ad first-read stats captured. Anthropic cost baseline updated. TikTok caption-policy change (max 5 hashtags) logged. Oula's outstanding "fleet number" issue closed no-action. No frontend code changes; no architecture-level structural changes.

## What Was Done

### 1. Session-start audit per CLAUDE.md handover-audit rule

Read `HANDOVER-2026-05-20.md` and cross-checked outstanding items:
- V 200 ad — confirmed posted by user this morning on TikTok + Instagram
- Sentry REACT-NATIVE-P issue — re-reviewed via screenshot, no diagnostic value yet (events still pre-v1.0.34, breadcrumb not deployed). Verdict unchanged from yesterday: archive recommended; re-open when v1.0.34 ships with breadcrumb attached.
- All other outstanding items verified.

### 2. Desktop TCC permission unblock (cross-update permission decay)

Bash + Read tools blocked from `~/Desktop` by macOS Full Disk Access — diagnosed as Claude Code's version-pinned binary path (`/Users/StephenLear/Library/Application Support/Claude/claude-code/2.1.142/claude.app`) requiring fresh FDA grant after each Claude Code update. User granted FDA + quit/relaunched Claude Code. Access restored. Same recurrence vector noted: every Claude Code update silently revokes Desktop access. Long-term fix would be granting FDA to Terminal.app (stable path) — deferred.

### 3. Stats 21/5 folder review (33 screenshots — dispatched to subagents)

Full review of `~/Desktop/stats 21:5/` (33 images). Two subagent passes handled the OCR work to keep main context clean.

**Headlines:**
- **Anthropic cost MTD: $206.66 across May 1-21 (~$9.84/day avg)** — no anomaly. Today's bar $8-10 (mid-pack). Mid-month peak ~$15-17 on May 9-11.
- **Cache hit rate 97.9% overall** (Sonnet 97.6%, Haiku 99.2%) — verified live on console after a subagent OCR false-alarm reported 57.5%. Caching is at asymptote, slightly better than May 7's 96.3%.
- **Anthropic credits $5.33 — auto-recharge confirmed ON** by user. No service-drop risk.
- **TikTok 7d: 10.6K views (+11.6% WoW), 5.6K total viewers, 46% returning. Poland share JUMPED 14.4% → 18%** in 24h — breakout signal.
- **iOS PL is now #3 country at 22 units (+6%)** behind DE (167) and UK (25). Europe 327 (+11%).
- **RevenueCat: 12 active subs, $41 MRR, $102 revenue (28d), 457 new customers, 508 active.** Play dominates revenue ($38 vs Apple $3 MRR).
- **V 200 ad first-read (12h post on TikTok): 506 views / 5.32s avg / 11.6% full-watch / 5 new followers / 98.6% For You / DE 71.5% / PL 5.5%.** Weak vs BR 101 v2's 21% benchmark. Hypothesis: visual-paradox formula needed a sharp contrast trigger (1962 vs 1996 = 34-year shock); V 200's 1962-still-running is continuity, not paradox.
- **V 200 IG: 163 views, 93% DE audience.** Low-quality cross-platform performance.

### 4. Polish ad strategy — winning formula identified

User referenced their **best-performing ad ever**: 2026-04-03 PKP Cargo / ET22 PINNED (URL `7624625253943151894`). Pre-existing memory entry confirmed: **1,222 views / 23% full-watch / 7.07s avg / 86.4% Poland / 50% returning viewers / pinned to profile**. Beats BR 101 v2 (21%) — channel's all-time best retention.

**What made 3/4 work (decoded by side-by-side analysis with May ads):**
1. **Product demo IN the ad** — actual LocoSnap UI screenshot with rarity badge visible. May ads (EN57 v1, ET22 v1) were pure footage with text overlays.
2. **Suspense hook in caption** ("LocoSnap mówi LEGENDARY — ale czy zgadł kl...") — curiosity gap.
3. **Rarity tier as punchline** (LEGENDARY / RARE / EPIC), not a stat number. Feels like a Pokémon-card pull.
4. **50% returning viewers** = app-demo hook converted viewers to brand-curious, not just video-watchers.

May 2026 PL ads abandoned this for pure-scarcity numbers ("ZOSTAŁO 60") and pure-survival framing ("Kiedy widziałeś?"). Both underperformed at 13.1% and lower. **SU46 reverses to the 3/4 winning shape.**

### 5. SU46 footage inventory + scan-demo frame selection

Three SU46 source clips found in `~/Desktop/SU46/`:
| Clip | Unit | Native specs | Use |
|---|---|---|---|
| SU46-029 Lubasz (special train) | 029 | 720×1280 / 60fps / 34s | **Best quality + hero shot** |
| SU46-031 PKP Cargo | 031 | 720×1280 / 25fps / 15s | Alt livery (Cargo blue) |
| SU46-047 platform arrival | 047 | 608×1080 / 30fps / 23s | Third livery (IC green-yellow) |

Three different units captured = visual variety while staying class-consistent.

Best scan-demo source frame identified via subagent: **`c1_06.jpg` ≈ t=10s into Lubasz clip**. Extracted at full resolution to `~/Desktop/SU46_scan_candidates/su46-029_t{09,10,11,12}s.jpg`. User used `t=10s` for the actual phone scan.

### 6. Polish endcard rebuilt at canonical path

Per memory `ad_assets_endcards.md`, the canonical endcards folder had been deleted between sessions. Built `endcard_pl.mp4` from scratch using the regeneration script in the memory file, with DE endcard frame as visual reference. Output: `/Users/StephenLear/Projects/locosnap/ad_assets/endcards/endcard_pl.mp4` (720×1280 / 30fps / h264 / 2.0s / 33KB).

**Polish text choices (mirror-translations of DE template):**
- Tagline (yellow): `Rozpoznaj każdy pociąg.` (imperative — matches DE `Jeden Zug erkennen.` infinitive tone)
- CTA line 1 (white): `Teraz w App Store` (matches `Jetzt im App Store`)
- CTA line 2 (white): `+ Google Play` (unchanged)

**Critical adjustment:** PL tagline at 44px overflowed. Dropped to **42px** to fit the longer Polish line ("Rozpoznaj każdy pociąg." = 23 chars vs DE's 19). Logged in memory.

Polish diacritics (`ż`, `ą`) render correctly via Arial Black + UTF-8 inline drawtext — no ASS file needed. Saved exact reproducible ffmpeg command to memory with verified PL strings inline.

### 7. SU46 PL ad built end-to-end

**File: `~/Desktop/locosnap_su46_pl.mp4` — 720×1280 / 30fps / h264 / 10.000s / 2.0MB.**

**Beat structure:**
| Beat | Window | Source | Text overlay |
|---|---|---|---|
| 1 | 0-2s | SU46-029 Lubasz t=9-11 (heritage green, wreath, snow) | `SU46.` (160px yellow) |
| 2 | 2-4s | SU46-031 PKP Cargo t=2-4 (blue, smoke billows) | `Diesel. / 1977.` (110+140px) |
| 3 | 4-6s | SU46-047 platform t=5-7 (green/yellow IC livery) | `Zostało tylko 20.` (72px) |
| 4 | 6-8s | User's screen recording t=18-20 (scan result page, RARE badge) | None — app UI is the message |
| 5 | 8-10s | `endcard_pl.mp4` (newly restored) | n/a |

Beat 3 was re-rendered after initial t=8 attempt produced a bumper close-up — fixed by re-shooting from t=5 (clip 3's hero-window per frame-sample c3_04).

**Backend correctness loop closed during build:** scan returned SU46 + RARE correctly, but spec sheet showed builder `H. Cegielski – Poznań` (wrong) while Historical Significance text in the same page credited Fablok + Pafawag (right). Fix shipped in `2a5db4e` (see next item) so future SU46 scans will display Fablok consistently across both panels.

### 8. Backend fix: SU46 builder override (commit `2a5db4e`)

`backend/src/services/trainSpecs.ts:674-680` — extended the existing SU46 `KNOWN_SPECS` override (which already locked `maxSpeed: "120 km/h"`) to also lock `builder: "Fablok Chrzanów"`. Applied to both `"su46"` and `"pkp su46"` lookup keys. All 9 trainSpecs tests pass. TypeScript clean.

Pushed to origin/main alongside the 2026-05-21 architecture-doc sync (`ffcec5c`). **Render auto-deploy triggered at session close.** Backend will reflect new builder on next SU46 scan after deploy (typically ~2 min).

Pattern continues: tester/ad correction → ship same session → reply when live.

### 9. Oula's "no readable fleet number" report — CLOSED no-action

Earlier today (pre-session), Oula reported an issue described in user's reply chain as "without a readable fleet number" — user had committed in DM: "Will deploy in the next hour. Thanks for the catch — please retest when I confirm it's live. Kiitos." `git log` showed no commits today before this session. User confirmed at session close: log as CLOSED no-action. Possible resolution off-band (UI clarification, misread, or out-of-scope decision). Logged in `backend_backlog_corrections.md` as CLOSED 2026-05-21.

**Same-day positive Oula feedback:** screenshot showed Oula confirming Dv12 scan correctly identified + facts accurate ("am happy that there were some not that well-known facts like 2700-seriers have been heavier in the past"). First in-field success report on the Finnish VR rules shipped 2026-04-18 — closes the validation loop on that work.

### 10. TikTok 5-hashtag rule discovered

User reported: when posting the V 200 ad this morning, TikTok's caption editor displayed "max 5 hashtags". **Confirmed in-product platform rule, not algorithmic guess.** Logged in `feedback_tiktok_reply_length.md` with prioritisation framework for cutting from >5 to 5 (class > brand > broad language tag > targeted community > cross-border bridge). Applies to all TikTok captions; Instagram and other platforms unchanged.

SU46 caption pre-cut to 5: `#SU46 #lokomotywa #polskiekoleje #trainspotting #LocoSnap`. Cuts: #fablok (too obscure), #pkpintercity #polregio (operators narrower than class), #pociąg (overlaps #lokomotywa), #kolej (overlaps #polskiekoleje), #zugspotter (DE term in PL post).

**Open question for next session:** does TikTok strip excess hashtags silently or hard-block the post until cut? V 200 morning post resolved before submission — unknown which behavior the UI enforces.

### 11. All docs saved + pushed

- `docs/CHANGELOG.md` — new 2026-05-21 entry for SU46 backend fix
- `docs/ARCHITECTURE.md` — header bumped to 2026-05-21 with SU46 fix + PL endcard + TikTok 5-tag rule cascaded into front-matter
- 5 memory files updated (see Important Files section)
- Two commits pushed: `2a5db4e` (backend fix) + `ffcec5c` (architecture sync)

## What Worked

- **Two-pass subagent stats review** — first agent identified the 3 cost screenshots from 33 images; second agent extracted numbers from the remaining 27. Kept main context clean while preserving all the data. Both agents ran <80s.
- **Live user-verification of subagent OCR** — saved a false memory-write. The 57.5% cache-hit-rate "regression" was OCR noise. User on Anthropic Console read off real numbers (97.9% overall) within seconds. Memory corrected before being committed. Lesson explicit in the updated `project_ai_cost_baseline.md`.
- **3/4 PINNED ad as the template anchor** — user surfacing this URL was the single most valuable framing of the session. Decoded the working hook structure (product-demo + suspense + rarity-tier) and applied it directly to SU46 without re-inventing.
- **Footage + scan reveal symmetry** — Beat 1 hero image (SU46-029 heritage green at Lubasz) is the SAME image used in Beat 4's scan-result page. Visual closure of the "did the AI get it right?" loop without needing additional text overlays.
- **Same-session backend fix on a correctness flaw discovered during ad build** — the SU46 Cegielski → Fablok fix was caught by the scan-test for the ad. Fix shipped before ad posted, so the caption's "Fablok Chrzanów" claim matches what users will see when they scan post-deploy.
- **File-based commit messages** (`/tmp/commit_msg.txt`) per CLAUDE.md — both commits used this. No heredoc Polish-character corruption.
- **Memory file restoration + script reproducibility** — `ad_assets_endcards.md` now contains the exact ffmpeg command with PL strings inline; future PL ad builds can regenerate the endcard from memory in one paste-and-run.
- **Frame-sampling at fps=1/2 for scan-source selection** — let subagent triage 17+8+12 frames to identify the best head-on scan-demo frame (`c1_06.jpg` / SU46-029 t=10s) without burning vision tokens reading each at full res.

## What Didn't Work (and Fixes)

- **Initial subagent OCR misread cache-hit rate as 57.5% from compressed screenshot.** The real value was 97.9%, not 57.5% — but the agent had no baseline awareness so it logged the false reading. **Fix:** when a subagent OCR's a number that contradicts a recent confirmed baseline (May 7's 96.3%), demand source-verification before logging. Memory file `project_ai_cost_baseline.md` now contains the lesson.
- **Beat 3 first pass was a bumper close-up.** Initial t=8 timestamp showed the SU46-047 too zoomed in (front bumper, side cab — not a hero shot). **Fix:** re-sampled clip at finer granularity, re-rendered from t=5 where the loco fills lower-middle frame head-on.
- **PL endcard tagline at 44px overflowed frame width.** "Rozpoznaj każdy pociąg." (23 chars) was too long for the DE-baseline 44px. **Fix:** dropped to 42px. Logged in memory as a per-locale tweak; DE stays at 44px, EN should fit at 44px (17 chars).
- **Desktop TCC blocked all file access at session start.** macOS Full Disk Access grants are per-binary-path; Claude Code's version-pinned path (`2.1.142/claude.app`) breaks every update. **Fix:** user re-granted FDA to the new binary + quit/relaunched. Logged the recurrence pattern; long-term fix is granting FDA to Terminal.app (stable path) but the user declined that wider grant for now.
- **Ignored task-tool reminders** appeared 6+ times during the session. These are not applicable to linear conversational/ad-build work — explicitly noted and skipped each time.

## Key Decisions

- **SU46 over EU07 / SU45 / EP05 as the next PL ad class.** SU46 won because (a) footage already on Desktop = zero sourcing time, (b) returns RARE in the app (good rarity badge for the hook), (c) three different units across three liveries available for visual variety, (d) "20 left" stat = compelling scarcity number, (e) no prior LocoSnap coverage on the channel.
- **2-second scan-reveal video clip over a static JPG for Beat 4.** User picked dynamic clip — more visceral than a frozen frame. Required iPhone Pro vertical 1206×2622 → 720×1280 scale-and-crop handling but rendered cleanly.
- **Three units across three beats** — used SU46-029 (heritage green), SU46-031 (Cargo blue), SU46-047 (IC green-yellow) to maximize visual variety while staying class-consistent. Beat 4 reveals the SAME SU46-029 image as Beat 1 to close the loop.
- **Backend SU46 fix shipped same session** rather than queued for backlog. The cost was 5 minutes of code + tests; the benefit is the SU46 ad's "did the AI get it right?" loop is fully consistent (class correct + builder correct + specs correct) once Render deploys.
- **5-hashtag prioritisation: class > brand > broad lang > targeted community > bridge.** Logged in memory as the standard cutting framework. Subjective but defensible — class identifier is the primary discovery vector for any specific-train post.
- **"Fablok Chrzanów" used in caption pre-deploy.** The caption claim matches what users will see in-app AFTER the Render deploy. Pre-deploy users will see Cegielski in the spec sheet but Fablok in the Historical Significance text — same inconsistency we just fixed. Risk: ~2-min window between ad post and Render deploy completion. Acceptable.
- **"Über 130" / "54 zbudowanych" wording for production count** — used the conservative 54 figure (Wikipedia standard). Source disagreement (53 vs 54) acknowledged in memory; if a Polish viewer pushes the 53 figure, reply pattern is "Stała racja — 53 lub 54 w zależności od źródła. Dziękuję!"
- **Oula's "fleet number" issue closed no-action** per user direction at session end. Not investigated further.
- **PL endcard tagline = "Rozpoznaj każdy pociąg." not "Każdy pociąg."** Older memory canonical was the short form; updated to the imperative form to match the DE infinitive "Jeden Zug erkennen." semantic register.

## Lessons Learned & Gotchas

- **Claude Code updates silently revoke Desktop FDA every release** — version-pinned binary paths (`/2.1.142/claude.app`) mean each update is a new TCC subject. Two options for permanent fix: (1) grant FDA to Terminal.app (stable path, broader scope), (2) tolerate the re-grant at each update. User chose the latter for now.
- **Cross-baseline OCR sanity check is mandatory** — subagent OCR of "57.5%" cache rate against a known prior of 96.3% should have triggered an immediate "verify before commit" flag, not a write-then-fix cycle. Lesson now in `project_ai_cost_baseline.md`.
- **TikTok captions are now 5-hashtag-capped** per in-product UI as of 2026-05-21. Affects every future TikTok post regardless of language. Channel-wide rule. Open question: silent strip vs hard block — verify next post.
- **Polish endcard requires fontsize 42 not 44** — "Rozpoznaj każdy pociąg." at 23 chars is the longest tagline across DE/EN/PL. DE at 19 chars and EN at 17 chars both fit at 44px. Logged.
- **The 3/4 PKP/ET22 PINNED ad is the channel's all-time retention benchmark (23% full-watch / 86.4% PL)** and the formula is reproducible: product-demo IN the ad + suspense hook + rarity-tier-as-punchline + scan-result-reveal beat. May 2026 PL ads abandoned this and underperformed.
- **`/tmp/su46_build/` working files** survive after the session but should be cleared periodically — `~/Desktop/SU46/` has the source clips, `~/Desktop/SU46_scan_candidates/` has the high-res scan frames, `~/Desktop/locosnap_su46_pl.mp4` has the final. The intermediate beats and frame checks at `/tmp/su46_build/` are reproducible from the build commands in this handover.
- **`ad_assets/` is never tracked in git by convention.** Memory file `ad_assets_endcards.md` documents this and provides the reproducible script. Untracked status is expected at session close — NOT a blocking issue.
- **Anthropic credits balance dropping to $5.33 is fine because auto-recharge is ON.** Confirmed by user. Daily-spend alert thresholds adjusted: $30 soft / $60 hard (was $50/$150 from old baseline).
- **Render deploy is in flight at session close** — backend SU46 fix won't be live in production for ~2 min after the push. If the user posts the SU46 ad in that window, early scans show Cegielski (pre-deploy); later scans show Fablok. Acceptable risk per the key-decision above.

## Next Steps (Prioritized)

1. **Post the SU46 PL ad** when ready. File: `~/Desktop/locosnap_su46_pl.mp4`. Caption (5 hashtags) above in §10 / in `tiktok_stats.md`. Add music in CapCut (~95-110 BPM Polish-instrumental cinematic, slower than V 200's heritage track; Beat 2's PKP Cargo blue + smoke fits an industrial-drum slot). Cross-post to Instagram with all 11 original hashtags (IG has no 5-tag limit).
2. **24h read on V 200 ad** — capture stats tomorrow morning (~9am). If full-watch still <15%, file the "visual paradox needs sharp contrast trigger" lesson formally and de-prioritise heritage-continuity hooks for the channel.
3. **Verify Render deploy completed** — re-scan the SU46-029 image in 5+ min from push (15:30 UTC push time, expect live by ~15:32). Spec sheet should show `Fablok Chrzanów`. If still Cegielski after 10 min, Render build may have failed — investigate Render dashboard.
4. **First-read stats on SU46 ad** — once posted, log TT and IG stats into `memory/tiktok_stats.md` (replace the "to populate" placeholder). Target benchmarks: TT PL share >50% (3/4 hit 86.4%, ET22 v1 hit 80%, EN57 v1 hit 65.7%); full-watch >15% (BR 101 v2 = 21%, 3/4 ET22 = 23%, V 200 12h = 11.6%).
5. **Sentry REACT-NATIVE-P archive** — at next convenient moment. No diagnostic value pre-v1.0.34 breadcrumb. Re-open if rate climbs on v1.0.34+.
6. **TikTok hashtag-cut behavior verification** — next time posting on TikTok with >5 hashtags in draft, observe: silent strip or hard block? Logged as open question in `feedback_tiktok_reply_length.md`.
7. **Watch for SU46 comment-thread corrections** on production-count (53 vs 54) or builder (Fablok vs Pafawag overlap). Reply pattern queued: "Stała racja — 53 lub 54 w zależności od źródła. Dziękuję!" / "Fablok plus Pafawag dla części — masz rację!"
8. **Future endcard rebuilds**: DE endcard still only in `~/Library/Mobile Documents/.Trash/locosnap_ad_build/endcard_de.mp4` (Trash). Restorable via the same script in memory with DE strings — do this before the next DE ad build.

## Important Files

| File | Role |
|------|------|
| [backend/src/services/trainSpecs.ts:674-680](backend/src/services/trainSpecs.ts) | SU46 `KNOWN_SPECS` override — now locks `builder: "Fablok Chrzanów"` AND `maxSpeed: "120 km/h"` for both `"su46"` and `"pkp su46"` keys |
| [docs/CHANGELOG.md](docs/CHANGELOG.md) | New 2026-05-21 entry: "Backend — SU46 builder correction (Cegielski → Fablok Chrzanów)" |
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | Header bumped to 2026-05-21 with SU46 fix + PL endcard + TikTok 5-tag rule cascaded into the front-matter chain |
| [ad_assets/endcards/endcard_pl.mp4](ad_assets/endcards/endcard_pl.mp4) | **NEW** — restored Polish endcard at canonical path. 720×1280/30fps/h264/2.0s/33KB. Untracked in git by convention. |
| `~/Desktop/locosnap_su46_pl.mp4` | **NEW** — final SU46 PL ad ready to post. 720×1280/30fps/h264/10.000s/2.0MB. |
| `~/Desktop/SU46_scan_candidates/su46-029_t{09,10,11,12}s.jpg` | **NEW** — high-res scan-demo source frames extracted from the Lubasz clip. t=10s was the user's pick. |
| `~/Desktop/SU46/` | Source footage (3 clips + the user's screen recording for Beat 4) |
| `/tmp/su46_build/` | Build workspace (beat[1-4].mp4, beat[1-4]_check.jpg, final_t[1,3,5,7,9].jpg, concat.txt) — intermediate, reproducible |
| [memory/ad_assets_endcards.md](/Users/StephenLear/.claude/projects/-Users-StephenLear-Projects-locosnap/memory/ad_assets_endcards.md) | Updated: PL endcard status RESTORED; full per-locale table (tagline + CTA line splits for DE/EN/PL); exact reproducible ffmpeg command for PL with all strings inline; per-locale font-size notes |
| [memory/tiktok_stats.md](/Users/StephenLear/.claude/projects/-Users-StephenLear-Projects-locosnap/memory/tiktok_stats.md) | New SU46 ad entry at top (BUILT, NOT YET POSTED) — full build notes, beats, caption, backend-correctness-loop, posting targets. V 200 entry updated with 12h-read stats. |
| [memory/project_ai_cost_baseline.md](/Users/StephenLear/.claude/projects/-Users-StephenLear-Projects-locosnap/memory/project_ai_cost_baseline.md) | New 2026-05-21 May-MTD section ($206.66 / 97.9% cache hit / 0 rate limits / credits $5.33 auto-recharge on). OCR false-alarm lesson captured. Spend-alert thresholds adjusted. |
| [memory/feedback_tiktok_reply_length.md](/Users/StephenLear/.claude/projects/-Users-StephenLear-Projects-locosnap/memory/feedback_tiktok_reply_length.md) | New section: TikTok caption hashtag limit max 5 (confirmed in-product 2026-05-21); 5-tag prioritisation framework; per-platform applicability noted |
| [memory/backend_backlog_corrections.md](/Users/StephenLear/.claude/projects/-Users-StephenLear-Projects-locosnap/memory/backend_backlog_corrections.md) | SU46 builder fix logged as SHIPPED 2026-05-21; Oula's "fleet number" issue logged as CLOSED no-action |

## Context for Next Session

- **Two commits pushed to origin/main today:** `2a5db4e` (backend SU46 builder fix), `ffcec5c` (architecture-doc sync). Both clean. **Render auto-deploy in flight at session close — backend should be LIVE in production for SU46 scans within ~2-5 minutes of the push.**
- **No frontend changes this session.** No EAS build needed. v1.0.33 still LIVE on both stores.
- **SU46 ad is BUILT but UNPOSTED.** File at `~/Desktop/locosnap_su46_pl.mp4`. Caption is umlaut-checked, Polish diacritics render correctly, 5-hashtag-limit applied. Ready for drag-into-TikTok/IG when user is ready. Music TODO in CapCut before posting.
- **Polish breakout is the dominant strategic signal this week** — TikTok 14.4% → 18% in 24h, iOS PL #3 country (+6%), v1.0.33 has PL in-app + PL Play listing + 40% PL annual cut LIVE. The SU46 ad is the highest-leverage next move per yesterday's handover plan; if it lands at >15% full-watch, the 3/4 PKP/ET22 visual-paradox-product-demo formula is confirmed repeatable.
- **TikTok captions now max 5 hashtags** — every future TT caption uses the cutting framework in `feedback_tiktok_reply_length.md`. Instagram unchanged.
- **Anthropic cost trajectory is healthy:** $206.66 MTD, 97.9% cache hit, zero rate limits, auto-recharge on, no anomaly from today's V 200 post. If a viral spike hits tomorrow, look for a step-change in the daily-cost chart and a possible cache-hit-rate dip (acceptable up to ~85%; below that = investigation).
- **Outstanding items from prior sessions (per audit):** V 200 24h read pending; SU46 ad post pending; Sentry REACT-NATIVE-P archive pending; Polish ad first-read stats target captures pending. None blocking.
- **Untracked file at handover close:** `ad_assets/` directory containing `endcard_pl.mp4` — expected per the convention documented in memory. NOT a blocking issue.
- **Worktree state on main:** clean. No active worktree-based work this session (all on main).
- **Memory files updated this session:** 5 — `ad_assets_endcards.md`, `tiktok_stats.md`, `project_ai_cost_baseline.md`, `feedback_tiktok_reply_length.md`, `backend_backlog_corrections.md`. Memory files are local-only — not in git.
