# Handover — 2026-06-18

## Status
Content + ops day, no code. Gacha-format validation gate read = FLOP (BR 103 Day 1) → diagnosed as the same frame-1 sin → **Day 2 Dragon rebuilt loco-first (v2), DE approved, NOT POSTED**. One Sentry issue triaged (RevenueCat transient, not a bug — user recovered + converted on €1). v1.0.39 live both stores; iOS v1.0.40 still an ASO draft pending a 1.0.40 build.

## What Was Done
- **Read BR 103 DE gacha (Day 1, posted 06-17 AM) at ~14h = FLOP.** 280 views / 2.86s avg / 7.1% full-watch / 0 followers / drop at 0:01 / For You 98.6%. Below the flop floor and worse than the scan-day flop (3.54s/8.4%). View curve flat after 5h = terminal.
- **Diagnosed root cause:** frame 1 was the app screen-recording (header/logo/buttons + big yellow „Kennst du die noch?" headline, loco dimmed in the reticle). The gacha asset structurally violates loco-first — its own footage never lets the loco dominate frame 1. So the *format* was never actually tested.
- **Rebuilt Day 2 Dragon loco-first (v2)** — 3 cuts DE/PL/EN, frame-checked every beat, NOT POSTED, DE approved by Stephen. New structure: bright lineside Dragon "Mania" hero in frame 1 (loco dominates, caption fades in after 0.8s) → scan/"Identifying" → RARE reveal card + reaction word (top dark band, card stats stay readable) → €1 endcard. 9.8s / 720×1280 / 30fps.
- **Wrote post captions** (DE/PL/EN) — collection-moat framing, no price push (endcard carries €1), no emoji, umlauts/diacritics verified, EN translations included.
- **Triaged Sentry REACT-NATIVE-D** "There was a problem with the App Store." = RevenueCat `STORE_PROBLEM` captured in `purchasePro` catch (`frontend/services/purchases.ts:200-218`). Handled, 1 organic non-tester user, 2 events/2mo, transient Apple-side. Not a bug — user recovered and converted on the €1 intro the same evening. Safe to Resolve/Archive.
- **Saved docs:** ARCHITECTURE.md date-bumped to 2026-06-18 (no architectural change); `tiktok_stats` memory updated with the flop read + v2 rebuild. No CHANGELOG entry (no frontend/backend code touched — ad files live on `~/Desktop`).

## What Worked
- **Frame-extract diagnosis before acting** — pulling frame 1 of the posted/old builds proved the app-chrome problem visually instead of guessing.
- **Two concrete source frames side by side** (bright lineside "Mania" vs brightened crop of own footage) made the permission-vs-quality tradeoff a clean decision for Stephen instead of an abstract debate.
- **Per-beat frame-check** — caught „SELTEN!" at y=980 covering the card's stats row (the gacha payoff detail); moved it to the top dark band and re-verified. The mandatory pipeline step earned its keep again.

## What Didn't Work (and Fixes)
- **The whole gacha format as originally built** — app screen-recording = app chrome in frame 1 = death at 0:01 (BR 103 flop). Fix: open on a clean loco-hero, sequence the scan/reveal as beats 2-3.
- **Own footage has no clean loco-hero source for Dragon** — the app scan recording keeps the loco dimmed inside the reticle even when cropped/brightened. Fix (Stephen's call): use the third-party "Mania" lineside clip for frame 1, permission risk accepted and noted.

## Key Decisions
- **Frame 1 must be a clean loco-hero, not the app UI.** The scan/reveal is the payoff (beats 2-3), never the cold open. This is the operative refinement of the loco-first rule.
- **Used the third-party "Mania" lineside clip for Dragon v2 frame 1** — Stephen accepted the permission risk (BR 140 v5 takedown precedent) because no own-footage loco-hero exists for this class.
- **No fake scarcity caption** — the reveal card's own "10 left" carries it; captions use collection-nudge framing.
- **Sentry STORE_PROBLEM = noise, not a fix.** Resolve/Archive. Code already handles cancel/pending correctly.

## Lessons Learned & Gotchas
- **Loco-first refinement, sharpened:** "is a train present in frame 1?" is not enough — "does the loco VISUALLY DOMINATE frame 1, or does app chrome / a headline?" A gacha asset that IS the app screen-recording can never pass this; it needs a separate loco-hero opener.
- **Dragon footage map:** bright loco-hero = `…/ad shots/EA6 Dragon/Dragon E6ACT-043 "Mania" …#pkp.mp4` (THIRD-PARTY, 720×1280). Own scan recording = `…/EA6 Dragon/ScreenRecording_05-05-2026 22-03-43_1.MP4` (1206×2622; "Identifying" ~t9-12, RARE reveal card ~t16-18+).
- **RevenueCat `STORE_PROBLEM`** surfaces as "There was a problem with the App Store." — transient Apple-side, usually recovers on retry. Optional backlog: suppress it from Sentry capture the way `PAYMENT_PENDING` already is (`purchases.ts:209`).
- **No untracked critical files** this session; working tree clean, nothing unpushed. No APK sent.

## Next Steps (Prioritized)
1. **Post Dragon v2** (DE/PL first, then EN) — this is the REAL first test of the loco-first gacha format. Confirm €1 intro still live. Post captions are drafted in the 06-18 chat / ready to paste.
2. **Read Dragon v2 hard at 24h** vs the validated band (~15-25% full-watch) before committing Day 3. If it clears, the format is validated; if it flops too, the gacha concept (not just the execution) is in question.
3. **Day 3 Class 37 (UNCOMMON)** — open question: keep as heritage/survivor UNCOMMON, or swap for a RARE/EPIC loco. Decide after Dragon v2's read.
4. **Resolve/Archive the Sentry STORE_PROBLEM issue.** Optional: confirm `dominik.kloepfer20@gmail.com` matches the evening €1 conversion in RevenueCat before closing.
5. **iOS v1.0.40 ASO draft still orphaned** — needs an EAS iOS build bumped to 1.0.40 + "What's New" before the staged metadata ships (`project_aso_state`). No build without explicit confirmation.

## Important Files
| File | Role |
|------|------|
| `~/Desktop/locosnap_gacha_dragon_{de,pl,en}_v2.mp4` | NEW — Day 2 Dragon loco-first rebuild, frame-checked, NOT POSTED |
| `~/Desktop/gacha_ads/build_dragon_v2.sh` | NEW — two-source build (lineside hero + own scan), reusable pattern for loco-first gacha |
| `~/Desktop/locosnap_gacha_br103_{de,pl,en}.mp4` | Day 1 BR 103 (DE posted = flop); BR 103 needs the same v2 treatment if revived |
| `~/Desktop/locosnap_gacha_class37_{de,pl,en}.mp4` | Day 3 Class 37 — pending the keep/swap decision; also needs v2 frame-1 fix if kept |
| `…/ad shots/EA6 Dragon/Dragon E6ACT-043 "Mania" …#pkp.mp4` | Bright loco-hero source (THIRD-PARTY, permission risk) |
| `…/ad shots/EA6 Dragon/ScreenRecording_05-05-2026 22-03-43_1.MP4` | Own scan recording (scan + RARE reveal card) |
| `frontend/services/purchases.ts:200-218` | `purchasePro` catch — where STORE_PROBLEM reaches Sentry; backlog: suppress like PAYMENT_PENDING |
| `docs/ARCHITECTURE.md` | Date-bumped 2026-06-18, no architectural change |
| memory `tiktok_stats.md` | BR 103 flop read + Dragon v2 rebuild logged |

## Context for Next Session
- **No code changed this session** → no `/changelog` entry (rule: changelog is frontend/backend code only). Ad files live on `~/Desktop`, not in the repo.
- **v1.0.39 live both stores. iOS v1.0.40 = ASO DRAFT only** — needs a 1.0.40 build.
- **Do NOT post any ad or trigger any EAS build without explicit confirmation** (`feedback_build_approval`).
- **Dragon v2 is an unvalidated-format test** — don't mass-produce v2 cuts of other classes until its 24h read is in.
- **Permission risk is live** on the Dragon "Mania" frame-1 clip — if a takedown/complaint arrives, swap frame 1 for the cropped own-footage version (lower quality but compliant) and re-post.
