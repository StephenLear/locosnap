# Handover — 2026-06-16

## Status
Content + strategy day, no code. Scan-day ad flopped → ran viral-train research + live verification → defined a new **gacha-rarity ad format** → **built a 9-ad batch (3 days × DE/PL/EN), NOT POSTED**. Competitor landscape investigated (all rivals are logbooks; AI-ID + collection is our whitespace). "Train Radar" idea added to backlog. No frontend/backend code changed → no `/changelog` entry. v1.0.39 live both stores; iOS v1.0.40 ASO draft still needs a 1.0.40 build to ship.

## What Was Done
- **Verified prior session close** (git/docs clean, all pushed) at session open.
- **Clapback ad POSTED 06-16** (TikTok + IG, €1 endcard, intro confirmed live). Records updated.
- **Scan-day ad 24h read = FLOP.** 916 views / 3.54s avg / **8.4% full-watch** / 1 follower; drop at 0:01; For You 98.6% (distributed fine = creative failure). Geo on-target (DE 65.6 / PL 12.7). **Lesson:** it WAS loco-first (BR 232 reticle frame 1) but a giant „EINEN TAG ZÜGE JAGEN" headline dominated frame 1 → read as a text-card open. Refinement banked in `feedback_loco_first_cold_open`: loco pixels aren't enough — the loco must VISUALLY DOMINATE frame 1.
- **Viral-train content research** — 4 parallel agents (DE railfan / global train formats / cross-niche pattern-breaks / ID-&-collection apps) + a **live TikTok pass** (Chrome). Convergent finding: the "unexpected twist" should BE the scan→rarity→collect reveal ("Shazam for trains"); authenticity beats production polish; loco must dominate frame 1.
- **Competitor teardowns** (all logbooks, none do AI-ID/rarity/collection):
  - **TrainSnap** (trainsnap.online) — UK logbook web app, tiny. Has a clever "Train Radar".
  - **Trainspotter** (Pete Eastwood, Google Play) — established UK number-logbook: 4.7★, 411 reviews, 10k+ downloads, free+ad-free, updated 3 Jun 2026. Makes the UK market harder; dictates UK positioning (lead on AI-ID/rarity/collection, never logging).
  - DelayCollector (adjacent), a German "Traintrack" (noted).
- **"Train Radar" feature idea added to `frontend_backlog`** (predict which classes pass your nearest station → daily-engagement driver).
- **BUILT the gacha-rarity ad batch — 9 ads, NOT POSTED** (Day 1 BR 103 EPIC approved; Day 2 Dragon RARE; Day 3 Class 37 UNCOMMON). Captions DE/PL/EN drafted for all three days. Reusable build script created.

## What Worked
- **Parallel research agents + live Chrome verification** — web breadth then ground-truth on actual TikTok view counts/frame-1 execution. Live data confirmed the loco-first lesson inside the niche (loco-dominant 1,093/847 likes vs talking-head 392 vs text-card 37).
- **App scan recordings as the gacha asset** — `ScreenRecording_*.mp4` files show viewfinder→rarity-card, the exact payoff, 100% own footage (zero permission risk, no coaches).
- **Per-beat frame-check** — caught „UNCOMMON." (9 wide caps) overflowing at FS2=112; rebuilt Class 37 at FS2=80. Mandatory pipeline step earned its keep.
- **Reusable env-parametrised build script** (`build_gacha.sh`) — one script, 9 builds, optional brightness for dark footage.

## What Didn't Work (and Fixes)
- **First loco picks were all too fresh** (ICE 1 06-12, BR 232 ×3 on 06-15/16, BR 218/143/155 in the 06-15 haul). User flagged it → applied the fresh-source check, re-picked from the larger `ad shots` library (BR 103, Dragon, Class 37).
- **BR 247 Vectron rejected** — reveals COMMON (kills the gacha payoff). On-trend but wrong tier.
- **Dragon viewfinder too dark** (dusk) → added `EQ1` brightness/contrast/saturation boost to beat 1.
- **„UNCOMMON." overflowed** at 112px → 80px. All-caps Arial Black is wider than the mixed-case char limits.
- **Class 37 mislabelled "RARE"** in the option I offered — it's actually UNCOMMON. Flagged honestly; copy reframed to heritage/survivor, swap offered.

## Key Decisions
- **New ad direction = loco-clean frame 1 → gacha-rarity reveal.** The differentiator (AI-ID + rarity + collection) IS the unexpected twist, not a separate gimmick.
- **Build 9 (3 concepts × DE/PL/EN), Day 1 first for sign-off** — user choices.
- **Avoid logbook positioning in the UK** — Trainspotter owns that, free. UK wedge = AI-ID/rarity/collection.
- **No false scarcity counts** — Dragon uses collection-nudge copy, not "only N left" (SU46 trap).

## Lessons Learned & Gotchas
- **Run the fresh-source check (pipeline step 8) BEFORE picking ad locos**, not after — grep `tiktok_stats` for recent usage of every candidate.
- **Loco must DOMINATE frame 1** — a big headline over the loco negates the loco-first advantage (the scan-day flop).
- **All-caps reaction words**: budget ~8 caps max at ~110px in Arial Black; frame-check every one.
- **Footage lives on the backup drive** `/Volumes/Steve backup/Locosnap/` — `LocoSnap_TrainVibez/` (clean per-class scan.mp4) and `locosnap_ad_build/ad shots/` (large library incl. ScreenRecording_*.mp4 per class). Use ONLY the ScreenRecording/scan recordings (own footage); the creator-named lineside clips are third-party (permission risk).
- **All train-app competitors are logbooks** — AI-ID + rarity + Pokémon-collect is genuine whitespace; risk is someone cracking it first.

## Next Steps (Prioritized)
1. **Post the gacha batch over the next 3 days** (DE/PL first, then EN). Confirm €1 intro still live each day. OR decide whether to **swap Day 3 Class 37 (UNCOMMON) → a RARE/EPIC loco** (open question).
2. **Validation gate:** read **Day 1 (BR 103)** hard at 24h vs the loco-first band (flop ~3.1s/8-12% vs validated 15-25%) BEFORE scaling the gacha format further.
3. **Clapback ad 24h read** due ~Wed 06-17 — watch for "zu teuer" pile-on.
4. **iOS v1.0.40 ASO draft still orphaned** — needs an EAS iOS build bumped to 1.0.40 + "What's New" filled before the staged metadata ships (`project_aso_state`). No build without explicit confirmation.
5. **Measure ASO lift ~2-4 weeks** after 1.0.40 ships + Play titles propagate.
6. Still-open infra (non-urgent): `storage.spot-photos` public-bucket fix; zero-engagement rescue push; Social Phase 1 QA.

## Important Files
| File | Role |
|------|------|
| `~/Desktop/locosnap_gacha_{br103,dragon,class37}_{de,pl,en}.mp4` | NEW — 9-ad gacha-rarity batch (Days 1-3), NOT POSTED |
| `~/Desktop/gacha_ads/build_gacha.sh` | NEW — reusable gacha-format build script (env-parametrised: SS/D/PTS/FS/EQ1) |
| `/Volumes/Steve backup/Locosnap/locosnap_ad_build/ad shots/<class>/ScreenRecording_*.mp4` | Per-class app scan recordings (the gacha asset; own footage) |
| `/Volumes/Steve backup/Locosnap/LocoSnap_TrainVibez/<class>/scan.mp4` | Clean per-class scan recordings (TrainVibez set) |
| `docs/handoffs/HANDOVER-2026-06-15.md` | 06-15 + early-06-16 record (clapback/scan-day reads, research, competitor teardown, gacha batch) |
| memory `tiktok_stats.md` | 06-16 gacha batch entry + scan-day flop read + clapback posted |
| memory `project_competitive_positioning.md` | TrainSnap + Trainspotter + DelayCollector teardowns; UK-positioning implication |
| memory `feedback_loco_first_cold_open.md` | Loco-must-dominate refinement + live verification |
| memory `frontend_backlog.md` | "Train Radar" feature idea added |

## Context for Next Session
- **No code changed this session** → no `/changelog` entry (rule: changelog is frontend/backend code only). `docs/ARCHITECTURE.md` unchanged — no build version, scan limit, service, schema, or distribution state changed.
- **v1.0.39 live both stores. iOS v1.0.40 = ASO DRAFT only, not submitted** — needs a 1.0.40 build.
- **Do NOT trigger any EAS build or post any ad without explicit confirmation** (`feedback_build_approval`).
- **The gacha batch is a VALIDATION test** of an unproven format — don't mass-produce more until Day 1's 24h read is in.
- Open question for next session: swap Day 3 Class 37 → RARE/EPIC loco, or keep as heritage UNCOMMON?
