# Handover — 2026-06-30

## Status
Productive session spanning 2026-06-29 evening into 06-30. Diagnosed (and archived) a low-severity Sentry error; read the DE ICE 1 rescue ad (partial win — format recovered, DE targeting nailed, still below the band); planned the next DE ad and landed on **BR 102** (Škoda 109E "Emil") from Samuel's footage; and the pre-build scan-verify caught a full BR 102 backend misID, which was **fixed, committed, and pushed** (`249600e`). Working tree clean, nothing unpushed. Backend deploy in flight on Render — needs a re-scan to verify before the ad is built.

## What Was Done
- **Sentry REACT-NATIVE-12 diagnosed + archived.** "Attempted to navigate before mounting the Root Layout component" from `handleScan` (`app/(tabs)/index.tsx`), Android 16, release 1.0.40 (33), **1 event / 1 user**, `handled: yes`. Root cause: the same Android-16/Hermes nav race `_layout.tsx` already guards everywhere else — `AuthGate` swaps the `<Stack>` for a spinner while auth re-loads (e.g. Activity restored mid-scan), so `router.push("/card-reveal")` throws while the root is unmounted. Scan data is safe (saved to History before the failing push); only artifacts are the Sentry noise + a raw error string shown to the user. Stephen **archived** it to watch for recurrence. Pre-diagnosed fix (a `safeNavigate` retry-on-tick helper) saved to memory `frontend_scan_nav_mount_race` — ship only on a 2nd event.
- **Read the DE ICE 1 rescue ad (~12h, posted 29/06 AM — NOT 28th as the prior handover said).** 729 views / **12.7% full-watch** / 4.94s avg / **10.2% like rate (best in the log)** / 8 saves / **71.2% Germany (most DE-concentrated cut in the log)**. Verdict: **partial win** — ~2-3× recovery over the broken Atlas format (4.4–7.8%), DE targeting nailed, content loved — but still ~3 pts below the tier-debate band floor (15.5%) because the "Basel curve-sweep" hero is a curve-approach, not a clean lateral pass, so it hit the same 0:01 drop as BR 111. Logged to `tiktok_stats`.
- **Planned the next DE ad → chose BR 102 (Škoda 109E "Emil").** Researched viral hooks (farewell-scarcity + Light&Honk win; Vectron confirmed too COMMON to be a hero) and subject strength. Checked footage: **thattrainbro is on Instagram, not TikTok** (memory corrected); his content is modern-corridor + Light&Honk (good B-roll, no disappearing classic). **Samuel (@trainspotting101, TikTok, "Altbau enjoyer")** is the disappearing-classic source. His BR 218 is **spent** (used recently). Settled on **BR 102** — fresh, distinctive, debate-worthy.
- **Frame-verified the BR 102 hero clip.** `Danke an den tf für die Makros 102 005 am RE1 rast sich richtung Münc….mp4` (Desktop): 102 005 on RE1 to München **with a horn salute** ("Makros"=horn). ffmpeg contact sheets show a distant approach 0–10s, then a **close/large lateral pass at ~10.7–13s** — hero in-point **~10.7s** (skip the 0–10s approach). This fixes exactly what the ICE 1 ad lacked.
- **BR 102 backend misID FIXED + SHIPPED (commit `249600e`, pushed to main → Render auto-deploys).** The scan-verify card returned **DB Fernverkehr / 160 km/h / 4,800 kW / COMMON / "103 left"** — every value wrong (class had zero backend grounding). Fixed across 5 services (specs/rarity/facts/vision/cache), web-verified vs de.wikipedia.org/wiki/DB-Baureihe_102. tsc clean, **270/270 tests**. Changelog + architecture synced.

## What Worked
- **The pre-build scan-verify gate.** Scanning BR 102 before building the ad caught a complete misID that would have shipped wrong specs on the card to a sharp DE audience. This is the BR 111 pattern (fix backend → re-scan → build) and it paid off again.
- **ffmpeg contact-sheet frame-sampling to find the hero in-point.** The 1fps then 4fps tiles cleanly exposed the distant-approach-then-lateral-pass structure and pinpointed ~10.7s. Right way to verify motion (not the web player).
- **Reading the per-country split, not the aggregate** (carried from 06-28) — the ICE 1 ad's 71% DE is the headline for the Germany rescue, visible only at country level.

## What Didn't Work (and Fixes)
- **ICE 1 ad still hit the 0:01 drop** despite being app-correct + loved. Cause: curve-sweep hero reads as an approach, not a lateral pass. → Next DE cut uses a clean lateral-pass hero (the BR 102 ~10.7s in-point).
- **TikTok rate-limited the profile grid** during the footage hunt ("Something went wrong"). → Stopped scraping (2-strike rule); relied on user-confirmed footage records + the clips Stephen pulled to Desktop, which is the real access path anyway (Samuel's clips are pulled on request).
- **thattrainbro mislabeled as TikTok in memory.** → Verified live he's Instagram-only (`instagram.com/thattrainbro`); `@thattrainbro` doesn't resolve on TikTok. Memory corrected.
- **BR 102 had zero backend grounding** → hallucinated specs/rarity/operator. → Added full KNOWN_SPECS/KNOWN_RARITY/facts/vision/cache (commit `249600e`).

## Key Decisions
- **Subject = BR 102, not Vectron or BR 146/101.** Vectron + 146 are COMMON (weak debate); 101 is done twice (fatigue). BR 102 is fresh, only 6 built (legit RARE), and has a built-in story (troubled "Emil" + 102 006 fire 2025).
- **Rarity = RARE, not EPIC.** Only 6 built is extraordinary, but it runs daily on one line (findable with effort), so RARE is honest and sets up the best "should be higher?" debate; EPIC would over-claim for a daily loco.
- **Pushed the backend fix immediately** (corrected card is a prerequisite for the ad). Confirmed via `git log origin/main..HEAD` = empty.
- **Hold the BR 102 ad build until the deploy is verified** with a fresh re-scan.

## Lessons Learned & Gotchas
- **Key BR 102 on "102" forms only, NEVER bare "109e"** — the Czech ČD 380 shares the Škoda 109E platform; a `109e` key would bleed identical specs onto a different operator/fleet.
- **Samuel's BR 218 is spent**; thattrainbro is **Instagram**, modern-corridor only (no disappearing classic). For DE disappearing-classic heroes, Samuel is the source and Stephen pulls the specific clip on request.
- **Frame-1 must be the close/large lateral pass** — the BR 102 clip buries its hero behind a 10s distant approach; the in-point (~10.7s), not the clip, is what matters.
- The DE ICE 1 ad actually posted **29/06 AM**, not 28th (TikTok card date + ~12h of data) — the 06-28 handover's "posted 28 Jun" was wrong.

## Next Steps (Prioritized)
1. **Verify the BR 102 deploy** once Render finishes — re-scan the 102 photo; the card must read **RARE / DB Regio / 200 km/h / 6,400 kW / 6 built** (not the old DB Fernverkehr / 160 / 4,800 / COMMON / "103 left"). Only build the ad after this passes.
2. **Build the BR 102 DE ad** — hero in-point ~10.7s (close/large lateral pass), keep raw audio (the horn salute = Light&Honk bonus), tier-debate hook ("only 6 in all of Germany — RARE — fair?"), credit **@trainspotting101**, lead caption with **#LocoSnap**. Per `feedback_ad_build_pipeline` + frame-check umlauts (München/Nürnberg).
3. **Await @_mikolkaa's reply** (PL ST44 "Gagarin" footage, DM sent 28 Jun) — still the only third-party thread. If silent another day, line up a backup PL creator from the IG #st44 grid.
4. **Watch the Sentry nav error** — ship the pre-diagnosed `safeNavigate` fix only on a 2nd event (`frontend_scan_nav_mount_race`).
5. **Do NOT post more Atlas cuts** (carried) — return to loco-first; Germany is the download priority (DE ▼68% iOS).

## Important Files
| File | Role |
|------|------|
| `~/Desktop/Danke an den tf für die Makros 102 005 am RE1 rast sich richtung Münc….mp4` | BR 102 hero source (Samuel); in-point ~10.7s, horn salute |
| `~/Desktop/ScreenRecording_06-29-2026 22-24-28_1.MP4` | The BR 102 scan-verify recording (showed the misID) |
| `backend/src/services/trainSpecs.ts` | BR 102 KNOWN_SPECS (200 km/h, 6,400 kW, Škoda, 6 built) |
| `backend/src/services/rarity.ts` | BR 102 KNOWN_RARITY = rare |
| `backend/src/services/trainFacts.ts` | BR 102 facts anchor (DB Regio, troubled intro, 006 fire) |
| `backend/src/services/vision.ts` | BR 102 vs ČD 380 + DB-Regio operator anchor |
| `backend/src/services/trainCache.ts` | BR 102 cache invalidation 2026-06-30 |
| `memory/backend_backlog_corrections.md` | BR 102 fix logged (shipped, pushed) |
| `memory/footage_source_trainspotting101.md` | 218 spent; BR 102 clip + in-point recorded |
| `memory/footage_source_thattrainbro.md` | Corrected to Instagram |
| `memory/frontend_scan_nav_mount_race.md` | Sentry nav error — watch item + ready fix |
| `memory/tiktok_stats.md` | DE ICE 1 ~12h read logged |

## Context for Next Session
- **Backend BR 102 fix is pushed** (`249600e`) — Render auto-deploys from main. The whole point is the corrected card; verify with a re-scan before building the ad (step 1).
- **The ad pipeline is: verify deploy → re-scan → build BR 102 ad.** Footage is on the Desktop and the in-point is known (~10.7s). This is the next concrete build.
- **Strategic frame (carried):** downloads are down because of Germany specifically; PL/UK healthy. Every DE content decision aims to recruit DE viewers with loco-first tier-debate + a clean lateral hero.
- All footage relationships: Samuel (@trainspotting101, TikTok) = blanket permission, credit required; thattrainbro (Instagram) = blanket permission on his own clips, credit required, modern-corridor/B-roll.
- Handover dated 2026-06-30 per system clock (session crossed midnight); one file per work day.
