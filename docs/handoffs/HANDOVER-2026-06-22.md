# Handover — 2026-06-22

## Status
**v1.0.40 "Spotting Atlas" SHIPPED — built, on-device-validated, submitted to both stores, now IN REVIEW** (Apple: In Review; Google: reviewing/"checking"). v1.0.39 remains the LIVE version until 1.0.40 clears. Android is a DRAFT production release — Stephen rolls it out after Google approves. Stephen did the final review submissions himself (standing preference).

## What Was Done
- **Train Radar Phase 2 → built, renamed to "Spotting Atlas", shipped.** New 5th bottom tab (`app/(tabs)/atlas.tsx`, `AtlasScreen`), native `react-native-maps@1.20.1` (`PROVIDER_DEFAULT` = Apple Maps iOS / Google Maps Android) consuming the migration-022 `get_spot_heatmap` RPC via new `fetchSpotHeatmap()`. Rarity-coloured dot markers, fine/coarse grid toggle (default 0.25°), legend, loading/empty states.
- **Sign-in gate** added — Atlas is FREE but requires sign-in (heatmap is communal collection data + RPC is auth-only). Signed-out users get a "Sign in to explore" teaser with Log In / Sign Up CTAs. Tab stays visible (sign-up pull).
- **Renamed Train Radar → Spotting Atlas** (tab "Atlas") — "Train Radar" was the competitor TrainSnap's term and implied live tracking; the feature is a historical communal heatmap.
- **Data correctness fix (migration 023, applied to prod):** `get_spot_heatmap` now filters to `verification_tier in ('verified-live','verified-recent-gallery')` — the same trust filter the leaderboard RPCs use. Caught by Stephen: gallery scans of EXIF-less photos record the user's *current device location* (home), not the train's, polluting the map with false hotspots. Verified-only counts: 18 cells (0.1°) / 28 cells (0.25°), down from ~80 pre-filter.
- **Place names via BigDataCloud** — `expo-location.reverseGeocodeAsync` returns nothing on many Android devices (device geocoder unreliable); swapped to BigDataCloud's keyless `reverse-geocode-client` (coarse cell centre only, never user location). Reliable on Android now.
- **Google Maps key** created + restricted: GCP project `locosnap-play-store` (free-trial billing linked), Application-restricted to `com.locosnap.app` + **both** SHA-1s (Play App Signing `76:0E…` for production + EAS keystore `EC:5E…` for test builds). In `app.json` `android.config.googleMaps.apiKey`. Safe in the public repo.
- **Stadler FLIRT builder fix** (backend, deployed to Render): IG tester sammyrox flagged "Stadler FLIRT 3XL von Siemens". Added `{ builder: "Stadler Rail" }` overrides for uncovered FLIRT keys + a specs-prompt rule + cache invalidation.
- **Built + submitted v1.0.40:** EAS production builds (Android AAB `40cf54be` vc 33, iOS `b3cfefb5` build 63), `eas submit` to both (Android service-account key + stored ASC API key, no passwords). Stephen submitted for review.
- **IG comments handled:** two PL paywall-friction replies (mechanism-first, then collection-first), the BR 101 ad's bodenspotter_jakobus (coverage Q) + sammyrox (FLIRT misID) replies drafted.
- **`eas.json` preview profile got `autoIncrement: true`** — ends the uninstall-before-install dance for future test builds.

## What Worked
- **Driving Google Cloud, Play Console, Supabase, and Expo via the Chrome MCP** — billing link, Maps key creation + restriction (both SHA-1s), migration 023 apply (paste via `pbcopy` → Cmd+V to avoid editor mangling), reading the App signing SHA-1.
- **Server-side data fix took effect immediately** — migration 023 fixed the pollution in already-installed builds (no rebuild needed for the data half).
- **Background build monitors** (poll `eas build:view --json` until finished) — auto-captured APK/IPA links.
- **On-device testing caught real bugs** the unit tests couldn't (red pins, "This area", stale-build installs).

## What Didn't Work (and Fixes)
- **Transparent Marker tap targets → Android default red pins.** A transparent custom marker view doesn't suppress Android's default pin → replaced with a visible rarity-coloured dot + `tracksViewChanges` toggle.
- **Place name always "This area".** First broadened `reverseGeocodeAsync` fields (insufficient — device geocoder returns nothing on Android) → switched to BigDataCloud.
- **New APK "updated" but kept old code.** EAS preview builds without `autoIncrement` reuse the version code, so Android silently kept the old build. Fix: uninstall-then-install (one-time), then added `autoIncrement` so future installs go in place. Diagnostic tell: the Atlas default grid (10 km = old build, 25 km = new build).
- **Maps key duplicate-key tsc error.** `"stadler flirt 3"` already existed in `WIKIDATA_CORRECTIONS` (BR 428 full specs) → dropped the duplicate, kept the existing one.
- **GCP/Play Console SPA deep-links bounce to home** — navigate via UI clicks or retry the deep link (it lands on the second try).

## Key Decisions
- **Native map (not a list), FREE, new tab, name "Spotting Atlas"** — all Stephen's calls.
- **Verified-only heatmap** (not camera-only) — matches the leaderboard's existing trust definition; thinner but accurate.
- **Default grid coarse (0.25°)** — denser first impression while verified data is thin.
- **Shipped despite thin data** — accurate-but-sparse beats dense-but-polluted; densifies over time.
- **Stephen does the final "submit for review"** — I do `eas submit` (uploads), he does the store-console review submission.

## Lessons Learned & Gotchas
- **Atlas place names depend on BigDataCloud** (external, keyless) — `expo-location` reverse-geocode is dead on Android. Don't revert.
- **The Maps key needs BOTH SHA-1s** — EAS keystore (test builds) AND Play App Signing (production). Both now registered. If the live Android map ever goes blank, check the key's Android restrictions first.
- **EAS preview needs `autoIncrement`** (now set) or Android won't update in place.
- **read IG and TikTok separately for the same ad** — the BR 101 gacha "died at 0:01" on TikTok but held a healthy retention curve on IG. (Also: the gacha format is retired in favour of tier-debate.)
- **Maps key is unrestricted-dangerous in a PUBLIC repo until locked** — it's now signature-restricted, so safe.

## Next Steps (Prioritized)
1. **Watch v1.0.40 review** — Apple (hours-to-a-day) + Google ("checking", usually <1 day). When Google approves, **Stephen rolls out the DRAFT production release**. When Apple approves, it auto-releases (or per release settings).
2. **After 1.0.40 is live:** confirm the Atlas works in the *production* (Play-distributed) Android app — that's the only build using the Play App Signing SHA-1 on the Maps key (test builds used the EAS one).
3. **v1.0.40 ASO draft** (`project_aso_state`) is now unblocked (it needed a 1.0.40 build) — apply the staged metadata.
4. **Post the ET22 PL tier-debate ad** (still queued from the 06-21 handover).
5. **Watch:** FLIRT re-scan confirmation (sammyrox); BigDataCloud place-name quality across regions; verified-only Atlas density as it grows.

## Important Files
| File | Role |
|------|------|
| `frontend/app/(tabs)/atlas.tsx` | The Spotting Atlas screen (map, dots, place names, sign-in gate, toggle) |
| `frontend/app/(tabs)/_layout.tsx` | Atlas tab registration (5th tab, map icon) |
| `frontend/services/supabase.ts` | `fetchSpotHeatmap()` RPC wrapper |
| `frontend/app.json` | `android.config.googleMaps.apiKey` + version 1.0.40 |
| `frontend/eas.json` | preview `autoIncrement: true` |
| `supabase/migrations/023_spot_heatmap_verified_only.sql` | verified-only heatmap filter (applied to prod) |
| `backend/src/services/trainSpecs.ts` + `trainCache.ts` | Stadler FLIRT builder fix + cache invalidation |
| `docs/design/train-radar-research.md` | Atlas design + status (renamed note at top) |

## Context for Next Session
- **Maps key, billing, both SHA-1s** all documented in memory `credentials_locations.md`.
- **eas submit needs no passwords** — Android service-account key (`/Users/StephenLear/locosnap-play-store-key.json`) + stored ASC API key in EAS. Stephen handles the final store-console review submission.
- **Release notes for 1.0.40** (EN/DE/PL) + an **App Review note** (explains the Atlas sign-in gate so reviewers can access it) were produced this session — in the chat transcript; reproduce if needed for the review submission.
- Working tree clean; all commits pushed to origin/main. Final commit `4d9e6ab` (docs); production bump `ceb96a0`.
- FLIRT fix is LIVE on Render now (backend) — independent of the 1.0.40 store review.
