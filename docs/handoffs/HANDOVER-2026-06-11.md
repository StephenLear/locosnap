# Handover — 2026-06-11

## Status
Big release-day session. **v1.0.39 BUILT + SUBMITTED to both stores — Android vc 31 went LIVE on Google Play the same evening (approved + published); iOS build 61 IN APPLE REVIEW. Migration 020 APPLIED + 3-step verified (prod schema now at 020 — Social Phase 1 is therefore ACTIVE for Android users as they update).** Full stats review of the "Stats 11:6" screenshot batch (27 dashboards): **Poland overtook Germany on every surface; FIRST €89.99 lifetime sale confirmed.** Three ads built and queued on the Desktop (Thu DE ICE 1, Fri PL EN57, Sat PL land-grab). Git clean, all pushed.

## What Was Done
- **v1.0.39 release pipeline, end to end:**
  - Pre-build verification: frontend 245/245 + tsc clean, backend 263/263 + tsc clean, EAS version-collision check (1.0.38 = iOS build 60 live → bump mandatory).
  - `frontend/app.json` 1.0.38 → 1.0.39 (`f698b79`); EN/DE/PL release notes at `docs/release-notes-v1.0.39.md` (415/492/408 chars; DE carries the extra "Baureihe" line; bug-fixes-first order).
  - EAS production builds finished in ~25 min: **iOS build 61 (`db0e7abb`), Android versionCode 31 (`a60bd6a8`)**.
  - **Migration 020 applied via SQL editor** (after full pre-apply audit: column refs, NOT-NULL default, grants, SECURITY DEFINER + pinned search_path, frontend RPC contract). Verified 3 steps with Stephen's account (`15f0c12a…`): profile RPC returns true counts (117 spots / 69 classes / 16 rare / 8 epic / 7 legendary); collection CSV shows ONLY the 10 public-safe columns (no lat/lng/photo_url); `is_public=false` → zero rows. **Stephen's `is_public` left FALSE** — flip via in-app toggle if his collection should be publicly visible at launch.
  - `eas submit` both platforms (iOS submission `0f8314e1`, Android `db350246`). **Session end state: Apple IN REVIEW (notes pasted), Play pre-launch checks running (draft per eas.json — rollout NOT yet started).**
- **Stats 11:6 review** (27 screenshots, 4 parallel extraction agents; all logged in `apple_stats` memory):
  - **PL overtook DE**: iOS units May 11–Jun 10 PL 45 (+543%) vs DE 35 (−58%, partly base effect vs the Apr DE ad wave); Android installs PL-dominated (Jun 1: 25 of 26); TikTok audience PL 63% / DE 22%.
  - **First lifetime sale**: €89.99 pro_lifetime, Play, German buyer, ~Jun 3 (logged in `project_lifetime_pro_demand`).
  - **Reprice survived**: 28.17% conversion on Jun 4 (reprice day) — second-tallest download day of the 90d window.
  - Play MAU 378 (+67%); Apple D1→D7 retention cliff unchanged (9-10% → 2%); pre-reprice €4.09 Play monthly cohort fully churned; new Play subs cluster at €1.00-1.30 intro.
  - **Open ops flag: Supabase Advisor shows 4 CRITICAL security findings** (pre-020 capture) — not yet investigated.
- **Three ads built** (full pipeline: frame-sampling, per-beat frame-checks; all 100% own footage + saved endcards; details + captions + EN translations in `tiktok_stats` memory):
  - **Thu AM: `locosnap_ice1_epic_de_v1.mp4`** — DE single-hero scan-reveal, ICE 1 EPIC tier-debate ("WAS ZIEHT DER ICE?" / "VERDIENT?"), annual DE endcard.
  - **Fri: `locosnap_en57_uncommon_pl_v1.mp4`** — PL mirror, EN57 UNCOMMON tier-debate ("TYLKO UNCOMMON?" / "ZA NISKO?"). Structurally safe: EN57 family rarity locked backend-side 06-09.
  - **Sat: `locosnap_pl_landgrab_v1.mp4`** — "POLSKA PRZEJMUJE LOCOSNAP" community pitch; beat 2 = real Polish all-time leaderboard from Stephen's fresh recording (LubieWoka #1, wall of PL flags). `_v0` (text-card beat 2) kept as fallback.
- **Memory corrections:** NEW `anthropic_credits_auto_refill` (credits auto-reload — low Console balance is NOT an alarm; Stephen had said this repeatedly but it was never saved); `feedback_translation_alongside_drafts` reinforced (overlay text needs EN translations too, not just captions); `project_market_focus` updated (PL-led acquisition on both platforms — "DE owns Android" now stale for installs).

## What Worked
- The pre-build skill checklist caught everything in order (version collision, tests, eas.json autoIncrement semantics) — zero wasted builds; both builds + both submissions first-try.
- Migration audit-before-apply (column refs vs real schema, NOT NULL default check, RPC contract vs frontend mappers) made the 020 apply a non-event.
- Ad pipeline discipline: every overflow/timing bug (3 total across 3 ads) was caught by frame-checks BEFORE the user saw anything.
- Parallel extraction agents for the 27-screenshot stats batch — exhaustive read with date-window discipline, main context kept clean.

## What Didn't Work (and Fixes)
- **Raised a false "top up Anthropic credits" alarm** — user has said many times credits auto-refill, but no memory file existed so every session re-derived the panic → memory file written, stats-log entry corrected.
- **Polish/German overlay text shipped without inline EN translations** — user had to ask → glossary provided, rule strengthened in memory.
- **Arial Black width traps recurred**: 9-char lines at 110-115px overflow 720px ("VERDIENT?", "UNCOMMON?") — downsized to 85-95px after frame-checks. The "≤10 chars at 110px" heuristic is too generous for wide-letter words (M/W/U-heavy).
- **Beat opened on a dead transition frame** (ICE 1 beat 2 at source t=8.8 caught the home-screen flash) — shifted to t=9.2. Frame-check the BEAT START, not just mid-beat.

## Key Decisions
- **Submit before Social QA**: builds went to stores same evening; Social Phase 1 QA happens via TestFlight while Apple reviews. Migration 020 was the gate and it cleared first.
- **Migration 021 NOT applied** — destructive operator retro-fix stays parked for its own diagnostic-first walkthrough with DB snapshot. The 020 verification CSV visibly shows the fragmentation it will fix (BR 403/412 under bare "DB (Deutsche Bahn)").
- **Ad strategy from the stats**: DE gets fed (two history flops in a row; market starving) but with the tier-debate format, not history lectures; PL rides momentum with the EN57 debate + the once-only land-grab pitch. Each day's post gates on the previous day's 24h read.
- **ICE 1 ad never shows the results page** — the app claims "37 built" there (wrong: 60 ICE 1 sets built); logged to `backend_backlog_corrections` with fix shape (KNOWN_SPECS number_built + optional KNOWN_RARITY epic lock since the ad drives ICE scans).

## Lessons Learned & Gotchas
- **Stephen's Country leaderboard tab defaults to the GERMAN board** (account country = DE) — any "Polish board" capture requires switching the country chip to PL mid-recording.
- **eas.json `appVersionSource: remote` + `autoIncrement`** = only `expo.version` in app.json needs a manual bump; build numbers handle themselves.
- The Supabase SQL editor only shows the last statement's result — run verification queries one at a time.
- EN57 scan recording (Apr 7) is still current post-rarity-lock (UNCOMMON pill) — old recordings stay usable when the displayed surface didn't change.

## Next Steps (Prioritized)
1. **DONE same evening — Android v1.0.39 LIVE on Google Play** (approved + published 2026-06-11 night). Apple side: watch the review.
2. **On-device Social Phase 1 QA — now possible immediately on Android** (update from Play): User A public → User B taps A on leaderboard → sees classes/rarity/blueprints, NO photos/location; toggle private → B sees private state. Flip Stephen's `is_public` to true if wanted.
3. **Post `locosnap_ice1_epic_de_v1.mp4` Thursday morning** (caption in `tiktok_stats`); 24h read Friday gates the EN57 post; Saturday land-grab v1. Eschede-comment posture: respectful, no product pitch.
4. **Investigate the 4 CRITICAL Supabase Security Advisor findings** (Dashboard → Advisors → Security).
5. **Small backend commit when convenient**: ICE 1 KNOWN_SPECS fix (number_built 60) + optional epic rarity lock + the pending BR 140 facts fix (2026-06-08 entry, "awaiting push").
6. **Migration 021 walkthrough** (operator retro-fix) — run the read-only DIAGNOSTIC block first, DB snapshot, leaderboard-shift warning.
7. Tier 3 backlog unchanged: custom profile photo, monthly→annual upsell.

## Important Files
| File | Role |
|------|------|
| `docs/release-notes-v1.0.39.md` | EN/DE/PL store notes — paste into Play Console + App Store Connect. |
| `supabase/migrations/020_social_public_profiles.sql` | APPLIED to prod 2026-06-11 + verified. Prod schema = 020. |
| `supabase/migrations/021_canonicalise_operators.sql` | NOT applied — destructive, diagnostic-first walkthrough pending. |
| `~/Desktop/locosnap_ice1_epic_de_v1.mp4` | Thursday DE ad (ICE 1 EPIC debate). |
| `~/Desktop/locosnap_en57_uncommon_pl_v1.mp4` | Friday PL ad (EN57 UNCOMMON debate). |
| `~/Desktop/locosnap_pl_landgrab_v1.mp4` (+`_v0` fallback) | Saturday PL ad (land-grab, real PL leaderboard beat). |
| `~/Desktop/ScreenRecording_06-11-2026 22-18-28_1.MP4` | Stephen's leaderboard recording (source for land-grab beat 2). |
| `~/Desktop/Stats 11:6/` | The 27 stats screenshots reviewed this session. |

## Context for Next Session
- **Git clean, all pushed.** Today's commits: `f698b79` (v1.0.39 prep), `93eb203` (020 applied + builds), `8a35084` (submitted both stores), plus this handover commit.
- **v1.0.39**: iOS build 61 IN APPLE REVIEW; Android vc 31 draft in Play Console, pre-launch checks running at session close. v1.0.38 remains the live build until rollout.
- **Prod Supabase schema = 020.** Social Phase 1 frontend+backend both live-capable; feature activates for users when v1.0.39 reaches them.
- **Stats snapshots all banked** in `apple_stats` / `project_market_focus` / `tiktok_stats` / `project_lifetime_pro_demand` memories — next capture ~2 weeks post-v1.0.39 to measure Social Phase 1 against tonight's baseline.
- **Anthropic credits AUTO-REFILL** — never raise a low-balance alarm again (`anthropic_credits_auto_refill` memory).
