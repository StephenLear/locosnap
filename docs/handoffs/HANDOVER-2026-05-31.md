# Handover — 2026-05-31

## Status
Tester-feedback + backend-correction session. **6 backend misID/spec fixes shipped + pushed to Render** (no app build — v1.0.35 still LIVE on both stores, no distribution/scan-limit change). Launched the **one-month straw poll**; 6 DM responses in (all would-recommend); replies drafted for all. Working tree clean, everything pushed. Memory + changelog + architecture all current.

## What Was Done
- **Reviewed Steph's feedback batch (IMG_5035-5038)** → two backend fixes shipped in `58ab2de`:
  - **SW1001 "Whatley Endeavour" No. 120 → Class 08**: the 2026-05-18 SW1001 rule keyed too hard on yellow Aggregate Industries livery, so the green/grey Whatley unit fell through. Made the rule livery-independent (centre-cab + bogies + quarry context decisive); added `whatley endeavour` spec key.
  - **John Fowler "Flying Falcon" (Works 4220016) → Class 03 / R&H 48DS**: new vision rule (0-4-0 vs 0-6-0 + cut-down cab + NIR context), 8-key KNOWN_SPECS (only verified NIR fields — builder/weight/gauge/diesel-hydraulic; power & maxSpeed left unset, NOT fabricated), trainFacts anchor. Specs verified against the NIR exhibit page.
- **Closed Eisenbahnfotograf_BLN** (BR 182→ÖBB 1116, ICE 4→ICE L) unactioned — no screenshots in the 5-day window.
- **Launched the one-month straw poll** (`AskUserQuestion` → testers + evangelists / short / free-text email): trilingual EN/DE/PL email sent to **22 testers via Resend** (`cbdeeefe-57f1-4788-945a-3437c20a3e41`, BCC, Reply-To `stephen@locosnap.app`) + 3-question DM to social evangelists (user posts those manually).
- **Market-research subagent** delivered a viral-mechanics brief (11 collection/ID apps): collection-first is right; the high-leverage new work is the *shareable outputs + FOMO/event layer* (Year-in-Spotting recap, rarity FOMO, region challenges, Untappd-style live feed). Validated the user's "30 trains" listicle idea but **reframed from scenic-travel → rarity/collection** ("rarest you can still catch in Germany").
- **Processed the DM straw-poll responses** (6 in) and shipped the backend items they surfaced:
  - **ÖBB 1216 Taurus III → BR 193** (`a31b92b`): real gap (we had 1116/1016, not 1216). vision cab-profile rule + 9 spec keys + trainFacts anchor (357 km/h record).
  - **Red Thalys/Eurostar → BR 408** (`a84d9f4`): screenshot (IMG_5041) revealed the reported "412→408" was actually a RED Thalys (BR 406) → white ICE. Added a STEP 0 livery gate (red high-speed → BR 406; white-ICE logic gated to white trains only).
  - **DRG E 77 → Škoda E 669.1** (`a84d9f4`): screenshot (IMG_5040, Czech gala) showed the existing rule wasn't catching — venue bias. Strengthened it: lead with rods-vs-bogies cue + a venue-bias guard.
  - **BR 159 fuel/spec** — see "What Didn't Work" below (shipped wrong, then corrected).
- **Drafted reply DMs** for all 6 responders (German BR 159 tester/Damian, Steph, Oula, Foxiar, R2-community, + Damian correction follow-up) and the Steph re-scan thank-you.
- **Logged everything**: changelog, architecture (header note items 1-6), backend_backlog_corrections, tester_contacts (poll send), project_tester_feedback (6-response tally), project_leaderboard_redesign (signals #2/#3/#4), backend_blueprint_generation_hang (re-opened), frontend_backlog (3 feature requests), project_status, new lesson memory.

## What Worked
- **Checking screenshots before fixing** — IMG_5041 ("412→408") was actually a red Thalys, a different/worse failure than described. Confirmed the discipline of not re-tightening existing rules blind.
- **Verified-fields-only KNOWN_SPECS** for Flying Falcon (left power/speed unset rather than fabricate) — matches the no-fake-specifics rule.
- **Fix-and-reply evangelist loop** continues to pay off — testers gave authoritative, specific corrections (Damian literally drives the BR 159).

## What Didn't Work (and Fixes)
- **BR 159 shipped WRONG, then corrected same day.** I assumed BR 159 = Siemens Vectron Dual Mode (the codebase's own vision note said so), entered 90 t / Bo'Bo' (`8987c2c`), and **dismissed the tester's "123 t" as physically impossible on a 4-axle loco**. Damian corrected: BR 159 is the **Stadler EuroDual** — a **Co'Co' six-axle** loco, so 123 t (~20.5 t/axle) is correct. My web "verification" had searched the wrong locomotive. Corrected in `55d60d2` (Stadler / Co'Co' / 123 t / 120 km/h / 7 MW elec + 2.8 MW diesel / bi-mode) across trainSpecs + vision note + trainFacts. The pre-existing `159 = Vectron` repo note was a latent error this caught.
  → **Lesson saved**: `feedback_verify_premise_before_overriding_expert.md` — verify a primary-source expert's premise (the loco identity) before overriding their figure.

## Key Decisions
- **Backend-only, no build** — all fixes are correction-layer; v1.0.35 stays live. No cache-version bump (class-string changes → fresh cache keys; avoids collateral wiping).
- **Held #1/#2 for screenshots** before fixing (both already had rules) — fixed precisely once IMG_5040/5041 arrived.
- **Reframed the listicle** away from scenic-travel toward rarity/collection (audience fit + research backing).
- **Leaderboard: build a scoring explainer (Path A.5), park Path B** — see below.

## Lessons Learned & Gotchas
- **Don't override a domain-expert primary source on a plausibility argument built on an unverified premise.** (BR 159 — full lesson in new memory.)
- **BCC sends hide per-recipient `last_event`** via the Resend MCP — only the `To:` recipient's status is visible; per-BCC-recipient delivery needs the dashboard.
- **Local git identity warning** appears on every commit (`StephenLear@MacBook-Air-4.fritz.box`) — harmless, commits/pushes succeed.

## Next Steps (Prioritized)
1. **Leaderboard scoring explainer (Path A.5).** Steph's 3rd+4th flags pinpoint the real problem: the **points formula is invisible** ("552 points, don't know how I got it"). Build a discoverable explainer that states the formula in plain words + breaks down the user's own total (rarity score = uncommon 2 / rare 5 / epic 8 / legendary 15). Small frontend. **Keep full Path B parked** — transparency, not tab-reordering, is what she needs. Confirm the exact breakdown wording with Steph.
2. **Blueprint-timeout investigation (RE-OPENED).** Now a 2-user, month-long signal (Christian + Oula): "Blueprint generation timed out." Investigate Replicate/DALL-E latency + the polling window (steps in `backend_blueprint_generation_hang.md`).
3. **EN57 rarity recalibration** (Foxiar) — Polish EMU under-rated; review the rarity override mechanism in `rarity.ts`.
4. **Feature requests** (logged, none quick): custom profile photo, loco-length spec field, scanning guide.
5. **IMG_5042 (1216) fleet number** — unconfirmed; the 1216 fix is cab-based so safe, but the number would confirm that specific loco.
6. **More poll responses** may still arrive → collate into `tester_product_feedback`.

## Important Files
| File | Role |
|------|------|
| `backend/src/services/vision.ts` | All 5 vision rules touched (SW1001, Fowler, 1216, red-Thalys STEP 0 gate, E 77, BR 159 note) |
| `backend/src/services/trainSpecs.ts` | KNOWN_SPECS: Whatley, Flying Falcon, 1216 (×9), BR 159 EuroDual (×7) |
| `backend/src/services/trainFacts.ts` | Anchors: Flying Falcon, 1216, BR 159 EuroDual |
| `docs/CHANGELOG.md` / `docs/ARCHITECTURE.md` | 2026-05-31 entries; arch header items (1)-(6) |
| `~/Desktop/feedback/IMG_5035-5042.PNG` | Steph batch (5035-5038) + DM screenshots (5040-5042: E669.1, red Thalys→408, blue→193) |
| `feedback_verify_premise_before_overriding_expert.md` (memory) | New lesson from the BR 159 error |
| `project_tester_feedback.md` (memory) | 6-response poll tally + emerging themes |
| `project_leaderboard_redesign.md` (memory) | Steph signals #3/#4 → scoring-explainer decision |

## Context for Next Session
Commits pushed today (origin/main, in order): `58ab2de` → `a31b92b` → `a84d9f4` → `8987c2c` → `55d60d2`. HEAD is `55d60d2`, working tree clean, nothing unpushed. All Render deploys are auto-triggered from main; no cache reset marker this session (no cache-version bump). 202/202 backend tests pass on HEAD. No frontend/build/EAS/Supabase change.

**Reply drafts are in the chat, not sent** — user posts DMs manually. Outstanding to post: the German/Damian BR 159 correction follow-up, Steph, Oula, Foxiar, R2-community, and the re-scan thank-yous. Once Render finishes deploying `55d60d2`, all 6 misID/spec fixes are live for re-scan.

The strongest strategic thread: **6/6 testers would recommend; strengths = collection + ease-of-use + detail; gaps = scan/rarity accuracy + community/discovery + leaderboard scoring clarity.** This maps cleanly onto the market-research brief — protect simplicity, add the social/transparency layer.
